---
phase: 04.1-cadastro-de-funcionarios
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db.ts
  - src/app/api/funcionarios/route.ts
  - src/app/api/funcionarios/[id]/route.ts
  - src/app/(main)/dashboard/funcionarios/page.tsx
  - src/app/(main)/dashboard/funcionarios/_components/funcionarios-table.tsx
  - src/app/(main)/dashboard/funcionarios/_components/funcionario-modal.tsx
autonomous: true
requirements: [SC-01, SC-02, SC-03, SC-04, SC-05]
user_setup: []

must_haves:
  truths:
    - "Usuário pode ver lista de funcionários em /dashboard/funcionarios com nome e status ativo/inativo"
    - "Usuário pode adicionar novo funcionário via modal — nome é obrigatório, duplicados bloqueados"
    - "Usuário pode editar nome de funcionário via modal — pré-preenchido, mesmas validações"
    - "Usuário pode ativar/desativar funcionário diretamente na tabela com atualização otimista"
    - "Dados persistem no SQLite após refresh — soft delete mantém dados no banco"
  artifacts:
    - path: "src/lib/db.ts"
      provides: "Migração da tabela funcionarios (id, nome, ativo, created_at, updated_at)"
      contains: "CREATE TABLE IF NOT EXISTS funcionarios"
    - path: "src/app/api/funcionarios/route.ts"
      provides: "GET (listar, aceita ?ativos=true) + POST (criar, valida nome obrigatório + duplicado case-insensitive)"
      exports: ["GET", "POST"]
    - path: "src/app/api/funcionarios/[id]/route.ts"
      provides: "PUT (update parcial: nome e/ou ativo) + DELETE (soft: set ativo=0)"
      exports: ["PUT", "DELETE"]
    - path: "src/app/(main)/dashboard/funcionarios/page.tsx"
      provides: "Server component — título 'Funcionários', renderiza FuncionariosTable"
      min_lines: 10
    - path: "src/app/(main)/dashboard/funcionarios/_components/funcionarios-table.tsx"
      provides: "Client component — tabela com busca, filtro, toggle, loading/error/empty states"
      min_lines: 300
    - path: "src/app/(main)/dashboard/funcionarios/_components/funcionario-modal.tsx"
      provides: "Modal reutilizável add/edit com validação inline, reset de estado, saving state"
      min_lines: 80
  key_links:
    - from: "funcionarios-table.tsx"
      to: "/api/funcionarios"
      via: "fetch em useEffect"
      pattern: "fetch\\(\"/api/funcionarios\"\\)"
    - from: "funcionarios-table.tsx"
      to: "/api/funcionarios/[id]"
      via: "fetch PUT para toggle ativo/inativo"
      pattern: "fetch\\(`/api/funcionarios/"
    - from: "funcionario-modal.tsx"
      to: "/api/funcionarios"
      via: "fetch POST (add) ou fetch PUT (edit)"
      pattern: "fetch\\(\"/api/funcionarios"
    - from: "page.tsx"
      to: "funcionarios-table.tsx"
      via: "import e renderiza FuncionariosTable"
      pattern: "FuncionariosTable"
---

<objective>
Implementar a página de gestão de funcionários com CRUD completo, soft delete e validação de duplicados.

**Purpose:** Permitir que o usuário cadastre, edite, ative/desative funcionários para usar nas fases seguintes (avaliação de desempenho).

**Output:**
- Migração SQLite da tabela `funcionarios`
- API endpoints CRUD (GET, POST, PUT, DELETE soft)
- Página `/dashboard/funcionarios` com tabela, busca, filtro e modal de formulário
- Sidebar item já existe — nenhuma modificação necessária

**User story:** **Como um** administrador, **eu quero** cadastrar e gerenciar funcionários com soft delete, **para que** possa avaliá-los individualmente nas fases seguintes.
</objective>

<execution_context>
@/home/jsilveira/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/jsilveira/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.1-cadastro-de-funcion-rios/04.1-CONTEXT.md
@.planning/phases/04.1-cadastro-de-funcion-rios/04.1-RESEARCH.md
@.planning/phases/04.1-cadastro-de-funcion-rios/04.1-PATTERNS.md
@.planning/phases/04.1-cadastro-de-funcion-rios/04.1-UI-SPEC.md
@.planning/phases/04.1-cadastro-de-funcion-rios/04.1-VALIDATION.md

<interfaces>
<!-- Key types extracted from existing codebase that executors will need -->

From src/navigation/sidebar/sidebar-items.ts:
```typescript
// Sidebar item "Funcionários" já existe (linhas 61-65):
{
  title: "Funcionários",
  url: "/dashboard/funcionarios",
  icon: Users,
}
// NENHUMA modificação necessária na sidebar
```

From src/lib/db.ts:
```typescript
export function getDb(): Database.Database // Singleton SQLite
// runMigrations() já existe dentro de getDb() — add nova CREATE TABLE lá
```

From src/app/api/carteira/route.ts (API route pattern):
```typescript
import { type NextRequest, NextResponse } from "next/server";
import { getDb } from "@/lib/db";
// GET: try/catch, searchParams, NextResponse.json()
// POST: body validation, prepared statements, error handling
```

From src/app/api/cil/[cil]/route.ts (Dynamic route pattern):
```typescript
export async function PUT(
  request: NextRequest,
  { params }: { params: Promise<{ id: string }> }
) { const { id } = await params; }
```

From src/app/(main)/dashboard/kpi/_components/carteira-modal.tsx (Modal pattern):
```typescript
"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/components/ui/dialog";
```

shadcn/ui components already installed: Card, Table, Dialog, Input, Button, Badge — all at `src/components/ui/`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar migração SQLite + API routes (GET, POST, PUT, DELETE soft)</name>

  <files>
    src/lib/db.ts (modify — add migration)
    src/app/api/funcionarios/route.ts (create)
    src/app/api/funcionarios/[id]/route.ts (create)
  </files>

  <read_first>
    src/lib/db.ts — ler função runMigrations() para saber onde adicionar nova CREATE TABLE
    src/app/api/carteira/route.ts — pattern de GET + POST com try/catch e prepared statements
    src/app/api/cil/[cil]/route.ts — pattern de dynamic route com params Promise
  </read_first>

  <action>
    **A — Modificar `src/lib/db.ts`:**
    Dentro de `runMigrations()`, APÓS a criação da tabela `carteira_clientes` (linha 36) e ANTES do índice `idx_pedidos_cil` (linha 39), adicionar:
    ```typescript
    database.exec(`
      CREATE TABLE IF NOT EXISTS funcionarios (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        nome TEXT NOT NULL,
        ativo INTEGER NOT NULL DEFAULT 1,
        created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
        updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
      )
    `);
    ```
    Schema conforme D-17 em `04-CONTEXT.md` e D4.1-17.

    **B — Criar `src/app/api/funcionarios/route.ts`:**
    Seguir pattern exato de `src/app/api/carteira/route.ts` (imports, try/catch, error handling).

    **GET** (per D4.1-13):
    - Ler `?ativos=true` de `searchParams`
    - Query: `SELECT id, nome, ativo, created_at, updated_at FROM funcionarios WHERE ativo = 1` (se `ativos=true`) senão sem WHERE
    - Ordenar por `ORDER BY nome COLLATE NOCASE`
    - Retornar array diretamente: `NextResponse.json(rows)`
    - Usar typagem explícita: `rows` como array, sem `any`

    **POST** (per D4.1-14):
    - Extrair `{ nome }` do body
    - Validar: nome deve ser string não vazia após trim
    - Se inválido → `{ error: "Nome é obrigatório" }` status 400
    - Verificar duplicado com `LOWER(nome) = LOWER(?)` — per Pitfall 1 da RESEARCH.md
    - Se existir → `{ error: "Já existe um funcionário com este nome" }` status 409 (conforme D4.1-14)
    - Inserir com `INSERT INTO funcionarios (nome) VALUES (?)`
    - Retornar `{ id, nome, ativo: 1 }` status 201
    - NÃO usar `ON CONFLICT` — tratamos duplicado explicitamente com verificação prévia

    **C — Criar `src/app/api/funcionarios/[id]/route.ts`:**
    Seguir pattern de `src/app/api/cil/[cil]/route.ts` com `params: Promise<{ id: string }>`.

    **PUT** (per D4.1-15):
    - Extrair `id` de params, converter com `Number(id)`, validar com `Number.isNaN()` → 400 se inválido
    - Aceitar body parcial: `{ nome }` OU `{ ativo: boolean }` OU ambos (conforme Pitfall 3)
    - Se `nome` presente: validar obrigatório, verificar duplicado excluindo próprio ID (`WHERE LOWER(nome) = LOWER(?) AND id != ?`)
    - Se `ativo` presente: converter para 0/1
    - Sempre atualizar `updated_at = CURRENT_TIMESTAMP`
    - Buscar registro atualizado após UPDATE: `SELECT id, nome, ativo, created_at, updated_at FROM funcionarios WHERE id = ?`
    - Se não encontrado → 404
    - Retornar registro atualizado

    **DELETE** (per D4.1-16 e D-15):
    - Soft delete: `UPDATE funcionarios SET ativo = 0, updated_at = CURRENT_TIMESTAMP WHERE id = ?`
    - Retornar `{ success: true }` status 200
    - Se ID não encontrado → 404

    **Import organization** (per PATTERNS.md):
    1. `import { type NextRequest, NextResponse } from "next/server";`
    2. Linha em branco
    3. `import { getDb } from "@/lib/db";`

    **Lint compliance:**
    - `useFilenamingConvention: "error"` → kebab-case já usado nos paths
    - `noFloatingPromises: "error"` → todas as promises tratadas com await + try/catch
    - `noCommonJs: "error"` → usar `import`/`export` ESM
  </action>

  <verify>
    <automated>
      npx biome check src/lib/db.ts src/app/api/funcionarios/route.ts src/app/api/funcionarios/ &amp;&amp; npx next build 2&gt;&amp;1 | tail -20
    </automated>
  </verify>

  <acceptance_criteria>
    1. `src/lib/db.ts` contém `CREATE TABLE IF NOT EXISTS funcionarios` com as colunas id, nome, ativo, created_at, updated_at
    2. `curl -s http://localhost:3000/api/funcionarios` retorna array JSON (vazio inicialmente)
    3. `curl -s -X POST http://localhost:3000/api/funcionarios -H 'Content-Type: application/json' -d '{"nome":"João"}'` retorna 201 com `{ id, nome, ativo: 1 }`
    4. `curl -s -X POST http://localhost:3000/api/funcionarios -H 'Content-Type: application/json' -d '{"nome":"joão"}'` retorna 409 com erro de duplicado
    5. `curl -s -X PUT http://localhost:3000/api/funcionarios/1 -H 'Content-Type: application/json' -d '{"ativo": false}'` retorna 200 com ativo=0
    6. `curl -s -X DELETE http://localhost:3000/api/funcionarios/1` retorna 200 com `{ success: true }` (soft delete)
    7. `curl -s "http://localhost:3000/api/funcionarios?ativos=true"` retorna apenas ativos
    8. `npx biome check src/lib/db.ts src/app/api/funcionarios/` passa sem erros
    9. `npx next build` passa sem erros
  </acceptance_criteria>

  <done>
    Migração SQLite executada, API endpoints funcionando com validação de duplicados case-insensitive, prepared statements, soft delete.
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar página server component + client table component com busca, filtro, estados</name>

  <files>
    src/app/(main)/dashboard/funcionarios/page.tsx (create)
    src/app/(main)/dashboard/funcionarios/_components/funcionarios-table.tsx (create)
  </files>

  <read_first>
    src/app/(main)/dashboard/kpi/page.tsx — pattern de server component page com título
    src/app/(main)/dashboard/kpi/_components/kpi-dashboard-client.tsx — pattern de client component com fetch + loading/error/empty states
    src/navigation/sidebar/sidebar-items.ts — confirmar que sidebar item já existe (não modificar)
    src/components/ui/card.tsx — API do Card
    src/components/ui/table.tsx — API do Table
    src/components/ui/input.tsx — API do Input
    src/components/ui/badge.tsx — API do Badge com variants default/secondary
  </read_first>

  <action>
    **A — Criar `src/app/(main)/dashboard/funcionarios/page.tsx`:**
    Seguir pattern de `kpi/page.tsx` mas simplificado — sem busca de sidebar label, título fixo.

    ```tsx
    import { FuncionariosTable } from "./_components/funcionarios-table";

    export default function FuncionariosPage() {
      return (
        <div className="flex flex-col gap-6">
          <div className="flex items-center justify-between">
            <h1 className="font-semibold text-2xl">Funcionários</h1>
          </div>
          <FuncionariosTable />
        </div>
      );
    }
    ```

    **B — Criar `src/app/(main)/dashboard/funcionarios/_components/funcionarios-table.tsx`:**
    Seguir pattern de `kpi-dashboard-client.tsx` para fetch, loading, error states.

    **Interface Funcionario:**
    ```typescript
    interface Funcionario {
      id: number;
      nome: string;
      ativo: number;
      created_at: string;
      updated_at: string;
    }
    ```

    **States do componente:**
    - `funcionarios: Funcionario[]` — dados carregados da API
    - `loading: boolean` — true durante fetch inicial
    - `error: string | null` — mensagem de erro se fetch falhar
    - `search: string` — texto da busca
    - `apenasAtivos: boolean` — filtro checkbox
    - `modalOpen: boolean` — controle do modal
    - `editingFuncionario: Funcionario | undefined` — undefined = add mode

    **Fetch inicial** (per Pattern 2 da RESEARCH.md):
    ```typescript
    useEffect(() => {
      void fetchFuncionarios();
    }, []);
    ```

    **Filter client-side** (per D4.1-05 e D4.1-06):
    - Case-insensitive: `f.nome.toLowerCase().includes(search.toLowerCase())`
    - Combinável com filtro ativos: `!apenasAtivos || f.ativo === 1`

    **Toggle ativo/inativo** (per D4.1-10 e UI-SPEC seção 4):
    1. Optimistic update: `setFuncionarios(prev => prev.map(f => f.id === id ? {...f, ativo: novoAtivo} : f))`
    2. Fetch `PUT /api/funcionarios/[id]` com `{ ativo: boolean }`
    3. Reverter em caso de erro
    4. **Sem refetch completo** — evitar flash na UI (conforme Anti-Pattern 1 da RESEARCH.md)

    **UI structure** (conforme UI-SPEC):

    ```
    <Card>
      <CardHeader>
        <div class="flex items-center justify-between">
          <CardTitle>Todos os Funcionários</CardTitle>
          <Button onClick={handleAdd}>
            <Plus className="size-4" />
            Adicionar Funcionário
          </Button>
        </div>
      </CardHeader>
      <CardContent>
        <!-- Search + filter bar -->
        <div class="mb-4 flex items-center gap-4">
          <div class="relative flex-1">
            <Search className="absolute left-2.5 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" />
            <Input placeholder="Buscar por nome..." value={search} onChange={...} className="pl-8" />
            {search && <button onClick={() => setSearch("")}>...</button>}
          </div>
          <label class="flex items-center gap-2 text-sm">
            <input type="checkbox" checked={apenasAtivos} onChange={...} className="size-4" />
            Só ativos
          </label>
        </div>

        <!-- Loading state -->
        {loading && <Loader2 className="size-6 animate-spin ..." />}

        <!-- Error state (conforme UI-SPEC copy: "Erro ao carregar funcionários. Tente recarregar a página.") -->
        {error && <div class="rounded-lg border border-destructive/50 bg-destructive/5 p-4">
          <p class="text-destructive text-xs">{error}</p>
          <Button variant="outline" size="sm" onClick={fetchFuncionarios}>Tentar novamente</Button>
        </div>}

        <!-- Table -->
        <Table>
          <TableHeader>
            <TableRow>
              <TableHead>Nome</TableHead>
              <TableHead>Status</TableHead>
              <TableHead class="text-right">Ações</TableHead>
            </TableRow>
          </TableHeader>
          <TableBody>
            {filtered.length === 0 ? (
              <TableRow>
                <TableCell colSpan={3} className="text-center text-muted-foreground">
                  Nenhum funcionário encontrado
                </TableCell>
              </TableRow>
            ) : (
              filtered.map(f => (
                <TableRow key={f.id}>
                  <TableCell className="font-medium">{f.nome}</TableCell>
                  <TableCell>
                    <Badge variant={f.ativo === 1 ? "default" : "secondary"}>
                      {f.ativo === 1 ? "Ativo" : "Inativo"}
                    </Badge>
                  </TableCell>
                  <TableCell className="text-right">
                    <div class="flex items-center justify-end gap-2">
                      <Button variant="ghost" size="icon-sm" onClick={() => handleEdit(f)} title="Editar">
                        <Pencil className="size-4" />
                      </Button>
                      <Button variant="outline" size="xs" onClick={() => handleToggleAtivo(f)}>
                        {f.ativo === 1 ? "Desativar" : "Ativar"}
                      </Button>
                    </div>
                  </TableCell>
                </TableRow>
              ))
            )}
          </TableBody>
        </Table>
      </CardContent>
    </Card>

    {modalOpen && <FuncionarioModal open={modalOpen} funcionario={editingFuncionario} onSave={handleModalSave} onClose={handleModalClose} />}
    ```

    **Import organization** (per PATTERNS.md):
    1. `"use client";`
    2. React imports (`useEffect, useState`)
    3. Linha em branco
    4. lucide-react imports (`Loader2, Pencil, Plus, Search, X`)
    5. shadcn imports (`@/components/ui/...`)
    6. Linha em branco
    7. Relative imports (`./funcionario-modal`)

    **Lint compliance:**
    - `noFloatingPromises: "error"` → `void fetchFuncionarios()` para fire-and-forget no useEffect
    - `useFilenamingConvention: "error"` → kebab-case: `funcionarios-table.tsx`
    - Todos os handlers de fetch com try/catch
  </action>

  <verify>
    <automated>
      npx biome check src/app/\(main\)/dashboard/funcionarios/ &amp;&amp; npx next build 2&gt;&amp;1 | tail -15
    </automated>
  </verify>

  <acceptance_criteria>
    1. Página `/dashboard/funcionarios` renderiza sem erro de build
    2. Título "Funcionários" visível (font-semibold text-2xl)
    3. Card "Todos os Funcionários" com botão "Adicionar Funcionário" com ícone Plus
    4. Input de busca com placeholder "Buscar por nome..." e ícone Search
    5. Checkbox "Só ativos" funcional — filtra client-side
    6. Loading state mostra Loader2 com animate-spin
    7. Error state mostra mensagem + botão "Tentar novamente"
    8. Empty state mostra "Nenhum funcionário encontrado"
    9. Tabela com colunas Nome (esquerda), Status (esquerda), Ações (direita)
    10. Badge "Ativo" com variant `default`, Badge "Inativo" com variant `secondary`
    11. Botão Editar com ícone Pencil (variant ghost, size icon-sm)
    12. Botão Ativar/Desativar (variant outline, size xs)
    13. npx biome check passa sem erros
    14. npx next build passa sem erros
  </acceptance_criteria>

  <done>
    Página de listagem funcional com busca, filtro, loading/error/empty states e toggle otimista.
  </done>
</task>

<task type="auto">
  <name>Task 3: Criar modal reutilizável add/edit + conectar fluxo completo</name>

  <files>
    src/app/(main)/dashboard/funcionarios/_components/funcionario-modal.tsx (create)
  </files>

  <read_first>
    src/app/(main)/dashboard/kpi/_components/carteira-modal.tsx — pattern de Dialog com formulário, saving state
    src/app/(main)/dashboard/kpi/_components/kpi-dashboard-client.tsx — pattern de abertura/fechamento/controle do modal
  </read_first>

  <action>
    Criar `src/app/(main)/dashboard/funcionarios/_components/funcionario-modal.tsx`:

    Seguir pattern exato de `carteira-modal.tsx` com as adaptações da RESEARCH.md e UI-SPEC.

    **Interface:**
    ```typescript
    interface FuncionarioModalProps {
      open: boolean;
      funcionario?: { id: number; nome: string }; // undefined = add mode
      onSave: (nome: string) => Promise<void>;
      onClose: () => void;
    }
    ```

    **Comportamentos (conforme UI-SPEC seção 3):**

    1. **Reset de estado ao abrir** (conforme Pitfall 2):
    ```typescript
    useEffect(() => {
      if (open) {
        setNome(funcionario?.nome ?? "");
        setError(null);
      }
    }, [open, funcionario]);
    ```

    2. **Validação client-side:**
    - Se `!nome.trim()` → `setError("O nome é obrigatório")`, não chama onSave
    - Desabilitar botão Guardar se `saving || !nome.trim()`

    3. **Saving state:**
    - `setSaving(true)` antes do fetch, `setSaving(false)` no finally
    - Botão mostra "Guardando..." durante saving
    - Cancelar e Guardar desabilitados durante saving

    4. **KeyDown Enter:**
    ```typescript
    onKeyDown={(e) => {
      if (e.key === "Enter") void handleSave();
    }}
    ```

    5. **Erro inline:**
    - Capturar erro do onSave (que pode vir do backend como 409)
    - Exibir em `<p className="text-destructive text-xs">`

    6. **Dialog close handling:**
    ```typescript
    onOpenChange={(open) => {
      if (!open) onClose();
    }}
    ```
    (Sem confirmação de perda de dados — aceitável para 1 campo, conforme UI-SPEC)

    **Estrutura JSX (conforme UI-SPEC):**
    ```
    <Dialog open={open} onOpenChange={...}>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>{isEdit ? "Editar Funcionário" : "Adicionar Funcionário"}</DialogTitle>
          <DialogDescription>
            {isEdit ? "Altere o nome do funcionário" : "Insira o nome do novo funcionário"}
          </DialogDescription>
        </DialogHeader>
        <div class="flex flex-col gap-4 py-2">
          <div class="flex flex-col gap-1">
            <label class="text-muted-foreground text-sm" htmlFor="nome-input">Nome</label>
            <Input id="nome-input" value={nome} onChange={...}
              placeholder="Nome do funcionário"
              onKeyDown={Enter → handleSave} />
          </div>
          {error && <p class="text-destructive text-xs">{error}</p>}
          <div class="flex justify-end gap-2">
            <Button variant="outline" onClick={onClose} disabled={saving}>Cancelar</Button>
            <Button onClick={handleSave} disabled={saving || !nome.trim()}>
              {saving ? "Guardando..." : "Guardar"}
            </Button>
          </div>
        </div>
      </DialogContent>
    </Dialog>
    ```

    **Conexão no `funcionarios-table.tsx`:**

    Já pré-estruturada na Task 2, verificar/completar os handlers:

    ```typescript
    function handleAdd() {
      setEditingFuncionario(undefined);
      setModalOpen(true);
    }

    function handleEdit(funcionario: Funcionario) {
      setEditingFuncionario(funcionario);
      setModalOpen(true);
    }

    function handleModalClose() {
      setModalOpen(false);
      setEditingFuncionario(undefined);
    }

    async function handleModalSave(nome: string) {
      const isEdit = editingFuncionario !== undefined;
      const url = isEdit ? `/api/funcionarios/${editingFuncionario.id}` : "/api/funcionarios";
      const method = isEdit ? "PUT" : "POST";

      const res = await fetch(url, {
        method,
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ nome }),
      });

      if (!res.ok) {
        const data = await res.json();
        throw new Error(data.error || "Erro ao salvar");
      }

      // Refetch completo após salvar (garante consistência)
      void fetchFuncionarios();
      handleModalClose();
    }
    ```

    **Lint compliance:**
    - `useFilenamingConvention: "error"` → kebab-case: `funcionario-modal.tsx`
    - `noFloatingPromises: "error"` → `void handleSave()` no onKeyDown, `void onSave(nome.trim())` no try/catch
    - Todos os imports organizados conforme padrão do projeto
  </action>

  <verify>
    <automated>
      npx biome check src/app/\(main\)/dashboard/funcionarios/ &amp;&amp; npx next build 2&gt;&amp;1 | tail -15
    </automated>
  </verify>

  <acceptance_criteria>
    1. Modal abre ao clicar "Adicionar Funcionário" — título "Adicionar Funcionário", descrição "Insira o nome do novo funcionário", campo vazio
    2. Modal abre ao clicar ícone de editar — título "Editar Funcionário", descrição "Altere o nome do funcionário", campo pré-preenchido
    3. Campo nome com placeholder "Nome do funcionário", label "Nome", htmlFor="nome-input"
    4. Enter no campo dispara submit
    5. Botão Cancelar fecha o modal
    6. Botão Guardar desabilitado se nome vazio
    7. Botão mostra "Guardando..." durante saving
    8. Erro 409 (duplicado) exibido inline em text-destructive text-xs
    9. Após sucesso: modal fecha, tabela atualiza via refetch funcŃionarios
    10. Ao reabrir modal, estado do input é resetado (Pitfall 2 resolvido)
    11. npx biome check passa sem erros
    12. npx next build passa sem erros
  </acceptance_criteria>

  <done>
    Modal reutilizável add/edit com validação, saving state, erro inline, reset de estado e conexão completa com API.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Input de nome do funcionário cruza de browser não confiável para API server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4.1-01 | Tampering - SQL Injection | `funcionarios/route.ts`, `funcionarios/[id]/route.ts` | mitigate | better-sqlite3 prepared statements com placeholders `?` — todas as queries usam `db.prepare(...).run(?)` ou `.get(?)`, nunca string interpolation. Já mitigado pelo pattern existente no projeto. |
| T-4.1-02 | Tampering - Mass Assignment | `funcionarios/[id]/route.ts` (PUT) | mitigate | Validar explicitamente apenas campos esperados (`nome` e/ou `ativo`). Ignorar campos extras no body. Não fazer `UPDATE SET ... = body.campo` genérico. |
| T-4.1-03 | Tampering - Integer Overflow/Bypass | `funcionarios/[id]/route.ts` | mitigate | Validar `Number(id)` com `Number.isNaN()` — retornar 400 se inválido. IDs não-inteiros ou NaN são rejeitados antes de qualquer query. |
| T-4.1-04 | Tampering - Duplicate Bypass via Case | `funcionarios/route.ts` (POST), `funcionarios/[id]/route.ts` (PUT) | mitigate | Usar `LOWER(nome) = LOWER(?)` em todas as verificações de duplicado. No PUT, excluir próprio ID: `WHERE LOWER(nome) = LOWER(?) AND id != ?`. |
| T-4.1-05 | Information Disclosure - Enumeration | `GET /api/funcionarios` | accept | Dashboard local sem autenticação — qualquer usuário com acesso à rede pode listar funcionários. Risco aceito: sem PII sensível (apenas nomes), sem autenticação no escopo do projeto. |
| T-4.1-06 | Tampering - Unauthorized Creation | `POST /api/funcionarios` | accept | Sem autenticação no projeto (out of scope per REQUIREMENTS.md). Risco aceito: dashboard local, single-user. |
</threat_model>

<verification>
## Validation Commands

```bash
# 1. Biome check em todos os arquivos novos/modificados
npx biome check \
  src/lib/db.ts \
  src/app/api/funcionarios/route.ts \
  src/app/api/funcionarios/\[id\]/route.ts \
  src/app/\(main\)/dashboard/funcionarios/page.tsx \
  src/app/\(main\)/dashboard/funcionarios/_components/funcionarios-table.tsx \
  src/app/\(main\)/dashboard/funcionarios/_components/funcionario-modal.tsx

# 2. Build completo
npx next build

# 3. Se build passar, iniciar dev server e testar API
# (manual) npx next dev
# (manual) curl -s http://localhost:3000/api/funcionarios
# (manual) curl -s -X POST http://localhost:3000/api/funcionarios -H 'Content-Type: application/json' -d '{"nome":"Teste"}'
# (manual) curl -s -X PUT http://localhost:3000/api/funcionarios/1 -H 'Content-Type: application/json' -d '{"ativo":false}'
# (manual) curl -s -X DELETE http://localhost:3000/api/funcionarios/1
</verification>

<success_criteria>
1. ✅ `npx biome check src/` passa sem erros em todos os arquivos modificados
2. ✅ `npx next build` passa — sem erros de tipo, lint ou runtime
3. ✅ Página `/dashboard/funcionarios` acessível — renderiza tabela (vazia inicialmente)
4. ✅ POST `/api/funcionarios` cria funcionário com nome e ativo=1 — retorna 201
5. ✅ POST `/api/funcionarios` com nome duplicado (case-insensitive) retorna 409
6. ✅ PUT `/api/funcionarios/[id]` com `{ nome }` atualiza nome
7. ✅ PUT `/api/funcionarios/[id]` com `{ ativo: false }` faz soft delete
8. ✅ DELETE `/api/funcionarios/[id]` faz soft delete (ativo=0)
9. ✅ GET `/api/funcionarios?ativos=true` retorna apenas ativos
10. ✅ Modal de adicionar abre, valida, salva e fecha
11. ✅ Modal de editar abre com nome pré-preenchido, salva e fecha
12. ✅ Toggle ativo/inativo na tabela funciona com atualização otimista
13. ✅ Busca por nome filtra em tempo real (client-side, case-insensitive)
14. ✅ Checkbox "Só ativos" filtra corretamente
15. ✅ Sidebar item "Funcionários" (grupo Avaliação e Desempenho) navega para `/dashboard/funcionarios`
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-cadastro-de-funcionarios/04.1-01-SUMMARY.md`
</output>

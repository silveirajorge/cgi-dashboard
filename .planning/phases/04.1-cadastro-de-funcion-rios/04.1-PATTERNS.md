# Phase 4.1: Cadastro de Funcionários — Pattern Map

**Mapped:** 2026-06-07
**Files analyzed:** 7 new / 2 modified
**Analogs found:** 7 / 7

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `src/app/api/funcionarios/route.ts` | api-route | CRUD | `src/app/api/carteira/route.ts` | exact |
| `src/app/api/funcionarios/[id]/route.ts` | api-route | CRUD | `src/app/api/cil/[cil]/route.ts` | exact |
| `src/app/(main)/dashboard/funcionarios/page.tsx` | component (server) | request-response | `src/app/(main)/dashboard/kpi/page.tsx` | exact |
| `src/app/(main)/dashboard/funcionarios/_components/funcionarios-table.tsx` | component (client) | CRUD | `src/app/(main)/dashboard/kpi/_components/kpi-dashboard-client.tsx` | role-match |
| `src/app/(main)/dashboard/funcionarios/_components/funcionario-modal.tsx` | component (client) | request-response | `src/app/(main)/dashboard/kpi/_components/carteira-modal.tsx` | exact |
| `src/lib/db.ts` (modify) | config/data | migration | `src/lib/db.ts` (existing migrated table blocks) | same-file |
| `src/navigation/sidebar/sidebar-items.ts` (verify) | config | — | `src/navigation/sidebar/sidebar-items.ts` (já existe) | same-file |

## Pattern Assignments

### `src/app/api/funcionarios/route.ts` (api-route, CRUD)

**Analog:** `src/app/api/carteira/route.ts`

**Imports pattern** (lines 1-3):
```typescript
import { type NextRequest, NextResponse } from "next/server";

import { getDb } from "@/lib/db";
```

> Note: Blank line between third-party and local imports per Biome `organizeImports` groups config (line 15-26 of biome.json).

**Core GET pattern** (lines 5-35):
```typescript
export async function GET(request: NextRequest) {
  try {
    const { searchParams } = new URL(request.url);
    const mes = searchParams.get("mes");

    const db = getDb();

    if (!mes) {
      return NextResponse.json({ error: "Parâmetro 'mes' é obrigatório (YYYY-MM)" }, { status: 400 });
    }

    const row = db.prepare("SELECT total_clientes FROM carteira_clientes WHERE mes_competencia = ?").get(mes) as
      | { total_clientes: number }
      | undefined;

    return NextResponse.json({ total_clientes: row?.total_clientes ?? null });
  } catch (error) {
    const message = error instanceof Error ? error.message : "Erro interno";
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
```

**Core POST pattern** (lines 37-73):
```typescript
export async function POST(request: NextRequest) {
  try {
    const body = await request.json();
    const { mes_competencia, total_clientes } = body;

    if (!mes_competencia || total_clientes === undefined) {
      return NextResponse.json(
        { error: "Campos obrigatórios: mes_competencia (YYYY-MM), total_clientes (number)" },
        { status: 400 },
      );
    }

    if (typeof total_clientes !== "number" || !Number.isInteger(total_clientes) || total_clientes < 0) {
      return NextResponse.json({ error: "total_clientes deve ser um número inteiro positivo" }, { status: 400 });
    }

    const db = getDb();
    db.prepare(
      `INSERT INTO carteira_clientes (mes_competencia, total_clientes)
      VALUES (?, ?)
      ON CONFLICT(mes_competencia) DO UPDATE SET
        total_clientes = excluded.total_clientes,
        updated_at = CURRENT_TIMESTAMP`,
    ).run(mes_competencia, total_clientes);

    return NextResponse.json({ success: true, mes_competencia, total_clientes });
  } catch (error) {
    const message = error instanceof Error ? error.message : "Erro interno";
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
```

**Adaptação para funcionarios GET** — trocar busca por `?ativos=true`, query com `ORDER BY nome COLLATE NOCASE`, retornar array diretamente.

**Adaptação para funcionarios POST** — validação `nome.trim()`, verificação duplicada com `LOWER(nome) = LOWER(?)`, retornar `{ id, nome, ativo }` com status 201.

---

### `src/app/api/funcionarios/[id]/route.ts` (api-route, CRUD)

**Analog:** `src/app/api/cil/[cil]/route.ts`

**Imports pattern** (lines 1-3):
```typescript
import { type NextRequest, NextResponse } from "next/server";

import { getDb } from "@/lib/db";
```

**Dynamic route param pattern** (lines 5-6):
```typescript
export async function GET(request: NextRequest, { params }: { params: Promise<{ cil: string }> }) {
  try {
    const { cil } = await params;
```

**Error handling pattern** (lines 40-43):
```typescript
  } catch (error) {
    const message = error instanceof Error ? error.message : "Erro interno";
    return NextResponse.json({ error: message }, { status: 500 });
  }
```

**Adaptação para PUT + DELETE** — usar `{ params }: { params: Promise<{ id: string }> }`, validar `Number(id)` com `Number.isNaN()`, suportar update parcial (`{ nome }` ou `{ ativo: boolean }`), soft delete via `UPDATE funcionarios SET ativo = 0`.

---

### `src/app/(main)/dashboard/funcionarios/page.tsx` (component-server, request-response)

**Analog:** `src/app/(main)/dashboard/kpi/page.tsx`

**Full pattern** (lines 1-17):
```typescript
import { sidebarItems } from "@/navigation/sidebar/sidebar-items";

import { KpiDashboardClient } from "./_components/kpi-dashboard-client";

const groupLabel = sidebarItems.find((g) => g.label === "Pedidos e Tendências")?.label ?? "Pedidos e Tendências";
const pageTitle = `Dashboard | ${groupLabel}`;

export default function KpiDashboardPage() {
  return (
    <div className="flex flex-col gap-6">
      <div className="flex items-center justify-between">
        <h1 className="font-semibold text-2xl">{pageTitle}</h1>
      </div>
      <KpiDashboardClient />
    </div>
  );
}
```

**Adaptação para funcionários** — simplificar: sem busca de sidebar label, título fixo "Funcionários", importar `{ FuncionariosTable }` em vez de `KpiDashboardClient`.

---

### `src/app/(main)/dashboard/funcionarios/_components/funcionarios-table.tsx` (component-client, CRUD)

**Analog:** `src/app/(main)/dashboard/kpi/_components/kpi-dashboard-client.tsx`

**Client component + fetch pattern** (lines 1-3, 42-51, 63-113):
```typescript
"use client";

import { useCallback, useEffect, useRef, useState } from "react";
```
```typescript
export function KpiDashboardClient() {
  const [data, setData] = useState<KpiDataResponse | null>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);
```
```typescript
  const fetchData = useCallback(async (fromVal: string, toVal: string) => {
    setLoading(true);
    setError(null);

    try {
      const [dataRes, cilRes] = await Promise.all([
        fetch(`/api/data?from=${fromVal}&to=${toVal}`),
        fetch(`/api/cil?from=${fromVal}&to=${toVal}`),
      ]);

      if (!dataRes.ok) throw new Error("Erro ao carregar dados");
      if (!cilRes.ok) throw new Error("Erro ao carregar CILs");

      const dataJson: KpiDataResponse = await dataRes.json();
      // ...
    } catch (err) {
      setError(err instanceof Error ? err.message : "Erro desconhecido");
    } finally {
      setLoading(false);
    }
  }, []);
```

**Loading + Error UI pattern** (lines 266-271, 288-293):
```typescript
  if (error) {
    return (
      <div className="flex items-center justify-center rounded-lg border border-destructive/50 bg-destructive/5 p-8">
        <p className="text-destructive">{error}</p>
      </div>
    );
  }
```
```typescript
      {loading ? (
        <div className="flex items-center justify-center py-16">
          <Loader2 className="size-8 animate-spin text-muted-foreground" />
          <span className="ml-2 text-muted-foreground">Carregando dados...</span>
        </div>
      ) : data ? (
        // ...
      ) : null}
```

**Modal open/close pattern** (lines 229-238, 329-337):
```typescript
  function handleCarteiraClick() {
    const mes = getMonthFromPeriod(from, to);
    setCarteiraMes(mes);
    setCarteiraModalOpen(true);
  }

  function handleCarteiraClose() {
    setCarteiraModalOpen(false);
    setCarteiraMes(null);
  }
```
```typescript
      {carteiraModalOpen && (
        <CarteiraModal
          open={carteiraModalOpen}
          mes={carteiraMes}
          currentValue={carteira}
          onSave={handleCarteiraSave}
          onClose={handleCarteiraClose}
        />
      )}
```

**Adaptação para funcionários** — fetch simples GET `/api/funcionarios`, filter client-side por `search` + `apenasAtivos`, optimistic update no toggle, shadcn Table com Badge para status, shadcn Card como container.

---

### `src/app/(main)/dashboard/funcionarios/_components/funcionario-modal.tsx` (component-client, request-response)

**Analog:** `src/app/(main)/dashboard/kpi/_components/carteira-modal.tsx`

**Full pattern** (lines 1-88):
```typescript
"use client";

import { useState } from "react";

import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";

interface CarteiraModalProps {
  open: boolean;
  mes: string | null;
  currentValue: number | null;
  onSave: (value: number) => void;
  onClose: () => void;
}

export function CarteiraModal({ open, mes, currentValue, onSave, onClose }: CarteiraModalProps) {
  const [valor, setValor] = useState(currentValue ?? 0);
  const [saving, setSaving] = useState(false);

  async function handleSave() {
    if (!mes) return;

    setSaving(true);
    try {
      const res = await fetch("/api/carteira", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ mes_competencia: mes, total_clientes: Math.round(valor) }),
      });

      if (!res.ok) throw new Error("Erro ao salvar");

      onSave(Math.round(valor));
    } catch {
      /* error handled silently */
    } finally {
      setSaving(false);
    }
  }
```

**Dialog render pattern** (lines 48-87):
```typescript
  return (
    <Dialog
      open={open}
      onOpenChange={(open) => {
        if (!open) onClose();
      }}
    >
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Carteira de Clientes &mdash; {mes ? formatMes(mes) : "---"}</DialogTitle>
          <DialogDescription>Atualizar o total de clientes na carteira para este mês</DialogDescription>
        </DialogHeader>

        <div className="flex flex-col gap-4 py-2">
          <div className="flex flex-col gap-1">
            <label className="text-muted-foreground text-sm" htmlFor="carteira-input">
              Total de Clientes
            </label>
            <Input
              id="carteira-input"
              type="number"
              min={0}
              step={1}
              value={valor}
              onChange={(e) => setValor(Number(e.target.value))}
            />
          </div>

          <div className="flex justify-end gap-2">
            <Button variant="outline" onClick={onClose}>
              Cancelar
            </Button>
            <Button onClick={handleSave} disabled={saving}>
              {saving ? "Guardando..." : "Guardar"}
            </Button>
          </div>
        </div>
      </DialogContent>
    </Dialog>
  );
}
```

**Adaptação para funcionário-modal** — add `useEffect` para resetar estado ao abrir (`[open, funcionario]`), props `funcionario?: { id: number; nome: string }`, `onSave: (nome: string) => Promise<void>`, validação inline de nome obrigatório, exibir erro no modal, `onSave` chama `fetch` POST ou PUT dependendo do modo.

---

### `src/lib/db.ts` (modify — add migration)

**Analog:** Same file, existing migration blocks (lines 17-42)

**Migration pattern** (lines 18-26):
```typescript
database.exec(`
  CREATE TABLE IF NOT EXISTS upload_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    filename TEXT NOT NULL,
    rows_imported INTEGER NOT NULL,
    rows_ignored INTEGER NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
  )
`);
```

**Nova migração a adicionar em `runMigrations()`** (após linha 42):
```typescript
database.exec(`
  CREATE TABLE IF NOT EXISTS funcionarios (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    nome TEXT NOT NULL,
    ativo INTEGER NOT NULL DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
  )
`);
```

---

### `src/navigation/sidebar/sidebar-items.ts` (verify — already exists)

**Status:** O item "Funcionários" já existe no grupo "Avaliação e Desempenho" (linhas 61-65):
```typescript
      {
        title: "Funcionários",
        url: "/dashboard/funcionarios",
        icon: Users,
      },
```

**Nenhuma modificação necessária.** O item está com `url` correta e sem `comingSoon`.

---

## Shared Patterns

### Error Handling Pattern
**Source:** `src/app/api/carteira/route.ts` (lines 31-34, 69-72)
**Apply to:** Both API route files
```typescript
  } catch (error) {
    const message = error instanceof Error ? error.message : "Erro interno";
    return NextResponse.json({ error: message }, { status: 500 });
  }
```

### Validation Pattern (backend)
**Apply to:** `src/app/api/funcionarios/route.ts`, `src/app/api/funcionarios/[id]/route.ts`
**Pattern:** Validate body fields manually (no Zod — only 1 field), return `NextResponse.json({ error: "mensagem" }, { status: 4XX })` for validation errors. Use `LOWER()` for duplicate checking in SQLite.

### File Naming Convention
**Source:** `biome.json` (line 77) — `useFilenamingConvention: "error"`
**Apply to:** All new files
**Rule:** All files must use **kebab-case**: `funcionario-modal.tsx`, `funcionarios-table.tsx`, not `funcionarioModal.tsx`.

### Floating Promises Rule
**Source:** `biome.json` (line 62) — `noFloatingPromises: "error"`
**Apply to:** All client component files
**Rule:** Use `void fetch(...)` for intentional fire-and-forget, or `.catch()` / `await` inside try/catch. Example from `kpi-dashboard-client.tsx` line 200:
```typescript
    void initializeData();
```

### Sonner Toast Pattern
**Source:** Project-wide convention (stack existente `sonner 2.0.7`)
**Apply to:** `funcionarios-table.tsx`, `funcionario-modal.tsx`
**Pattern:** Use `toast.success("Mensagem")` / `toast.error("Erro")` for user feedback on save/delete/toggle.

### Import Organization
**Source:** `biome.json` (lines 15-26) + observed convention
**Apply to:** All files
**Pattern:**
1. React imports
2. Blank line
3. Next.js imports
4. Blank line
5. Third-party packages (lucide-react, sonner, etc.)
6. Blank line
7. Alias imports (`@/components/...`, `@/lib/...`)
8. Blank line
9. Relative imports (`./_components/...`)

## No Analog Found

| File | Role | Data Flow | Reason |
|---|---|---|---|
| — | — | — | Todos os 7 arquivos têm analog exato ou role-match |

## Metadata

**Analog search scope:** `src/app/api/`, `src/app/(main)/dashboard/kpi/`, `src/lib/`, `src/navigation/`, `src/components/ui/`
**Files scanned:** 8 analog files + 3 UI component files
**Pattern extraction date:** 2026-06-07

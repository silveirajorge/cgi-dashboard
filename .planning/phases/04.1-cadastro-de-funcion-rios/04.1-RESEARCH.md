# Phase 4.1: Cadastro de Funcionários — Research

**Researched:** 2026-06-07
**Domain:** CRUD com soft delete — Next.js App Router + SQLite + shadcn/ui
**Confidence:** HIGH

## Summary

Esta fase implementa a página de gestão de funcionários com CRUD completo, soft delete e validação de duplicados. O código segue patterns já estabelecidos nas Fases 1-3: server component page + client component table, API routes no App Router, SQLite com better-sqlite3, e modais com shadcn Dialog.

**Primary recommendation:** Seguir o padrão `carteira-modal.tsx` para o modal de adicionar/editar, o padrão `kpi-dashboard-client.tsx` para o client component com fetch/loading/error state, e o padrão `carteira/route.ts` para as API routes com validação e error handling.

**Sidebar item já existe** (`src/navigation/sidebar/sidebar-items.ts`, grupo "Avaliação e Desempenho", título "Funcionários", URL `/dashboard/funcionarios`, ícone `Users`) — o item está presente com `comingSoon: false`, apenas a página e API precisam ser criadas.

<phase_requirements>
## Phase Requirements

| ID | Descrição | Suporte da Pesquisa |
|----|-----------|---------------------|
| SC-01 | Página `/dashboard/funcionarios` com tabela | Pattern `kpi/page.tsx` + `kpi-dashboard-client.tsx` confirmado como modelo |
| SC-02 | Botão "Adicionar Funcionário" com formulário | Modal pattern do `carteira-modal.tsx` com Dialog + Input |
| SC-03 | Toggle ativar/desativar (soft delete) | Badge clicável ou Switch — ver componentes disponíveis |
| SC-04 | Tabela SQLite `funcionarios` | `runMigrations()` em `db.ts` — schema D-17 da Fase 4 |
| SC-05 | API endpoints CRUD | Pattern `carteira/route.ts` + `cil/[cil]/route.ts` confirmados |
</phase_requirements>

<user_constraints>
## User Constraints (from CONTEXT.md)

### Locked Decisions (D4.1-01 a D4.1-17)

Ver `04.1-CONTEXT.md` para lista completa. Decisões-chave:
- Página em `/dashboard/funcionarios`, server + client pattern
- Sidebar item já existe (grupo "Avaliação e Desempenho", ícone `Users`)
- Tabela com colunas: Nome, Status (Ativo/Inativo), Ações
- Busca por nome + filtro "Só ativos"
- Modal Dialog para adicionar/editar
- Toggle ativo/inativo direto na tabela
- Nome obrigatório, duplicados bloqueados (case-insensitive, backend)
- API: GET /api/funcionarios, POST, PUT /api/funcionarios/[id], DELETE (soft)
- Schema da tabela `funcionarios` conforme D-17

### the Agent's Discretion
- Ícone específico (`Users` da lucide-react)
- Layout exato da tabela (largura das colunas)
- Texto dos placeholders e botões
- Cor do badge Ativo/Inativo

### Deferred Ideas (OUT OF SCOPE)
Nenhuma.
</user_constraints>

## Architectural Responsibility Map

| Capability | Primary Tier | Secondary Tier | Rationale |
|------------|-------------|----------------|-----------|
| Listagem de funcionários | Browser (Client) | API (Backend) | `funcionarios-table.tsx` faz fetch client-side, renderiza tabela com shadcn Table |
| Busca/filtro em tempo real | Browser (Client) | — | Filtragem client-side do array já carregado (não refetch) |
| Adicionar funcionário | Browser (Client) | API (Backend) | Modal abre no client, envia POST para API, API valida e persiste |
| Editar funcionário | Browser (Client) | API (Backend) | Modal pré-preenchido, envia PUT para API |
| Soft delete / toggle ativo | Browser (Client) | API (Backend) | Switch/badge clicável envia PUT com `{ ativo: boolean }` |
| Validação duplicados | API (Backend) | — | Query case-insensitive com `LOWER()` no SQLite |
| Persistência SQLite | API (Backend) | — | `getDb()` + prepared statements em `funcionarios/route.ts` |

## Standard Stack

### Core — Já existente no projeto, nenhuma instalação nova necessária

| Library | Version | Purpose | Why Standard |
|---------|---------|---------|--------------|
| Next.js | 16.2.7 | App Router, API routes, server components | Stack existente |
| better-sqlite3 | — | SQLite via singleton `getDb()` | Stack existente (Fase 1) |
| shadcn/ui Dialog | — | Modal de adicionar/editar | Pattern `carteira-modal.tsx` |
| shadcn/ui Table | — | Listagem de funcionários | Pattern `cil-modal.tsx` |
| shadcn/ui Badge | — | Status Ativo/Inativo | Componente já instalado |
| shadcn/ui Input | — | Campo de nome no formulário | Componente já instalado |
| shadcn/ui Button | — | Ações (Guardar, Cancelar) | Componente já instalado |
| shadcn/ui Card | — | Container da página | Pattern KPI dashboard |
| lucide-react | 1.17.0 | Ícone `Users` (sidebar), `Plus`, `Pencil` | Stack existente |
| sonner | 2.0.7 | Toast para feedback de sucesso/erro | Stack existente |

### Alternatives Considered

| Instead of | Could Use | Tradeoff |
|------------|-----------|----------|
| Modal (Dialog) para add/edit | Página dedicada separada | Modal é mais rápido e consistente com pattern existente; página separada é overkill para 1 campo |
| react-hook-form + Zod | useState simples | O formulário só tem 1 campo (nome); useState é suficiente e consistente com `carteira-modal.tsx` |
| Server Action para CRUD | API routes | API routes são o pattern estabelecido (Fases 1-3) e já têm error handling consistente |

**Versões verificadas:** Nenhum pacote novo precisa ser instalado — todos os componentes shadcn/ui necessários (Dialog, Table, Badge, Input, Button, Card) já estão em `src/components/ui/`.

## Architecture Patterns

### System Architecture Diagram

```
Usuário abre /dashboard/funcionarios
  │
  ▼
page.tsx (Server Component)
  │  renderiza título "Funcionários"
  │
  ▼
funcionarios-table.tsx (Client Component)
  │
  ├── useEffect → fetch("GET /api/funcionarios")
  │     │
  │     ▼
  │   API Route: src/app/api/funcionarios/route.ts
  │     │  db.prepare("SELECT * FROM funcionarios ORDER BY nome")
  │     │
  │     ▼
  │   Response: [{ id, nome, ativo, created_at, updated_at }, ...]
  │
  ├── [Barra de busca + checkbox "Só ativos"]
  │     │  filtrar array local (client-side)
  │
  ├── [Tabela com linhas de funcionários]
  │     ├── Coluna Nome: texto
  │     ├── Coluna Status: Badge "Ativo" / "Inativo"
  │     └── Coluna Ações: [Editar] [Toggle ativo/inativo]
  │
  ├── [Botão "Adicionar Funcionário"]
  │     │
  │     ▼
  │   Modal (Dialog) — AddFuncionarioModal
  │     ├── Input: nome
  │     ├── Botão Cancelar
  │     └── Botão Guardar → fetch("POST /api/funcionarios", { nome })
  │           │
  │           ▼
  │         API: valida nome obrigatório + duplicado (LOWER)
  │           ├── 409 → exibir erro no modal
  │           └── 201 → fechar modal, refetch lista
  │
  └── [Ícone de editar na linha]
        │
        ▼
      Modal (Dialog) — EditFuncionarioModal (mesmo componente, reutilizado)
        ├── Input: nome (pré-preenchido)
        ├── Botão Cancelar
        └── Botão Guardar → fetch("PUT /api/funcionarios/[id]", { nome })
              │
              ▼
            API: valida nome obrigatório + duplicado (excluindo próprio ID)
              ├── 409 → exibir erro no modal
              └── 200 → fechar modal, refetch lista

[Toggle ativo/inativo na linha]
  │
  ▼
fetch("PUT /api/funcionarios/[id]", { ativo: 0/1 })
  │
  ▼
API: UPDATE funcionarios SET ativo = ?, updated_at = CURRENT_TIMESTAMP
  │
  ▼
Response 200 → atualizar estado local (sem refetch completo)
```

### Recommended Project Structure

```
src/
├── app/
│   ├── api/
│   │   └── funcionarios/
│   │       ├── route.ts              # GET (listar), POST (criar)
│   │       └── [id]/
│   │           └── route.ts          # PUT (atualizar nome/ativo), DELETE (soft)
│   └── (main)/
│       └── dashboard/
│           └── funcionarios/
│               ├── page.tsx          # Server Component
│               └── _components/
│                   ├── funcionarios-table.tsx    # Client Component principal
│                   └── funcionario-modal.tsx     # Modal add/edit (reutilizável)
├── lib/
│   └── db.ts                        # + migração tabela funcionarios
└── navigation/
    └── sidebar/
        └── sidebar-items.ts         # Já existe, verificar item
```

### Pattern 1: Server Component Page + Client Component Table

**What:** Página server component minimalista que renderiza título e delega toda a interatividade a um client component co-locado.

**When to use:** Toda página com dados dinâmicos, CRUD, formulários ou interatividade.

**Source:** `src/app/(main)/dashboard/kpi/page.tsx` [VERIFIED: codebase]

```tsx
// page.tsx
import { FuncionariosTable } from "./_components/funcionarios-table";

export default function FuncionariosPage() {
  return (
    <div className="flex flex-col gap-6">
      <div className="flex items-center justify-between">
        <h1 className="font-semibold text-2xl">Funcionários</h1>
      </div>
      <FuncionariosTable />
    </div>
  );
}
```

### Pattern 2: API Route com Error Handling

**What:** Next.js App Router route pattern com try/catch, validação e response padronizado.

**When to use:** Toda API route.

**Source:** `src/app/api/carteira/route.ts` [VERIFIED: codebase]

```ts
import { type NextRequest, NextResponse } from "next/server";
import { getDb } from "@/lib/db";

export async function GET(request: NextRequest) {
  try {
    const { searchParams } = new URL(request.url);
    // ... lógica
    return NextResponse.json({ data });
  } catch (error) {
    const message = error instanceof Error ? error.message : "Erro interno";
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
```

### Pattern 3: Dynamic Route (PUT/DELETE por ID)

**What:** API route with `[id]` param pattern.

**Source:** `src/app/api/cil/[cil]/route.ts` [VERIFIED: codebase]

```ts
export async function PUT(
  request: NextRequest,
  { params }: { params: Promise<{ id: string }> }
) {
  const { id } = await params;
  // ...
}
```

### Pattern 4: Modal Dialog com Formulário

**What:** shadcn Dialog component com formulário, estado de saving e fetch.

**Source:** `src/app/(main)/dashboard/kpi/_components/carteira-modal.tsx` [VERIFIED: codebase]

**When to use:** Qualquer modal com formulário de 1-2 campos.

```tsx
"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";

interface FuncionarioModalProps {
  open: boolean;
  funcionario?: { id: number; nome: string }; // undefined = add mode
  onSave: (nome: string) => void;
  onClose: () => void;
}
```

### Anti-Patterns to Avoid

- **Fazer refetch completo ao toggle ativo/inativo:** Em vez disso, atualizar o estado local do item específico para evitar flash na UI
- **Validar duplicados só no client:** A validação case-insensitive DEVE ser no backend (SQLite `LOWER()`) — o client-side é bônus
- **Criar sidebar item novo:** O item "Funcionários" já existe em `sidebar-items.ts` grupo "Avaliação e Desempenho" — não duplicar
- **Usar DELETE real:** A decisão D4.1-16 e D-15 especificam soft delete (`ativo=0`), não remoção física

## Don't Hand-Roll

| Problem | Don't Build | Use Instead | Why |
|---------|-------------|-------------|-----|
| Modal overlay/animations | CSS custom modal | shadcn Dialog | Acessibilidade (Radix), animações, focus trap, keyboard dismiss — já instalado |
| Tabela com estilos | CSS custom table | shadcn Table | Responsivo, consistente com tema, hover states, já instalado |
| Badge de status | CSS custom chip | shadcn Badge | Variants `default`/`secondary`/`destructive`/`outline` mapeiam perfeitamente para ativo/inativo |
| Validação de duplicados | Lógica só no frontend | Backend com `LOWER()` | SQLite `=` é case-sensitive; `LOWER(nome) = LOWER(?)` garante case-insensitive |

**Key insight:** Este CRUD é tão simples (1 campo editável + 1 toggle) que useState é suficiente — react-hook-form + Zod seria overkill para um formulário de nome único. O padrão `carteira-modal.tsx` com fetch manual é o modelo correto.

## Common Pitfalls

### Pitfall 1: Case-Insensitive Duplicate Check no SQLite

**What goes wrong:** `SELECT COUNT(*) FROM funcionarios WHERE nome = ?` não detecta "João" como duplicado de "joão" porque SQLite `=` em TEXT é case-sensitive.

**Why it happens:** SQLite usa `BINARY` collation por padrão para `=`.

**How to avoid:** Usar `LOWER(nome) = LOWER(?)` na query de verificação de duplicados. Ao editar, excluir o próprio ID da verificação: `WHERE LOWER(nome) = LOWER(?) AND id != ? AND ativo = 1`.

**Warning signs:** Usuário cadastra "João" e consegue cadastrar "joão" — duas entradas para a mesma pessoa.

### Pitfall 2: Modal Reutilizável — Reset de Estado

**What goes wrong:** Ao reutilizar o mesmo modal para adicionar e editar, o campo de nome não reseta entre aberturas.

**Why it happens:** O estado `useState` do input persiste entre renders; ao fechar e reabrir, o valor antigo ainda está lá se não for resetado.

**How to avoid:** Usar `key` prop no modal baseado no modo (add vs edit) para forçar remontagem, ou criar um `useEffect` que reseta o estado quando `open` muda para `true`.

```tsx
// Reset ao abrir
useEffect(() => {
  if (open) {
    setNome(funcionario?.nome ?? "");
    setError(null);
  }
}, [open, funcionario]);
```

### Pitfall 3: PUT Aceitando Campos Parciais

**What goes wrong:** O PUT endpoint precisa aceitar `{ nome }` (editar nome) OU `{ ativo: boolean }` (toggle) OU ambos. Se a validação exigir sempre `nome`, o toggle quebra.

**Why it happens:** Schema de validação que torna todos os campos obrigatórios.

**How to avoid:** Validar no backend: se `nome` estiver presente, validar nome obrigatório + duplicado; se `ativo` estiver presente, validar boolean. Ambos são opcionais, mas pelo menos um deve estar presente.

### Pitfall 4: Biome `useFilenamingConvention: "error"`

**What goes wrong:** Arquivos com nomes CamelCase como `funcionarioModal.tsx` quebram o lint.

**Why it happens:** O biome.json tem `useFilenamingConvention: "error"` na categoria `style`.

**How to avoid:** Usar kebab-case para todos os arquivos: `funcionario-modal.tsx`, `funcionarios-table.tsx`.

### Pitfall 5: `noFloatingPromises: "error"` — Biome Nursery Rule

**What goes wrong:** `fetch(...)` sem `await` ou `.catch()` dispara erro de lint.

**Why it happens:** Regra `noFloatingPromises` no biome.json.

**How to avoid:** Usar `void fetch(...)` quando o fire-and-forget é intencional, ou sempre `.catch()` / `await` em try/catch.

### Pitfall 6: Atualizar `updated_at` nos Updates

**What goes wrong:** Ao fazer toggle ativo/inativo ou editar nome, o campo `updated_at` não é atualizado.

**How to avoid:** Incluir `updated_at = CURRENT_TIMESTAMP` em todas as queries UPDATE, não apenas nos INSERTs.

### Pitfall 7: Modal Fechar sem Salvar ao Clicar Fora

**What goes wrong:** Usuário preenche o formulário, clica fora do modal acidentalmente, perde os dados.

**How to avoid:** Por default o Dialog fecha ao clicar fora (click outside) e ao pressionar Escape. Se desejado, usar `DialogPrimitive.Root` com `onOpenChange` para confirmar antes de fechar. O pattern atual (`carteira-modal.tsx`) usa `onOpenChange` para chamar `onClose` sem confirmação — para 1 campo é aceitável.

## Code Examples

### API Route — GET + POST /api/funcionarios

```typescript
// src/app/api/funcionarios/route.ts
// Pattern: src/app/api/carteira/route.ts [VERIFIED: codebase]

import { type NextRequest, NextResponse } from "next/server";
import { getDb } from "@/lib/db";

export async function GET(request: NextRequest) {
  try {
    const { searchParams } = new URL(request.url);
    const apenasAtivos = searchParams.get("ativos") === "true";

    const db = getDb();
    const rows = db
      .prepare(
        `SELECT id, nome, ativo, created_at, updated_at
         FROM funcionarios
         ${apenasAtivos ? "WHERE ativo = 1" : ""}
         ORDER BY nome COLLATE NOCASE`,
      )
      .all();

    return NextResponse.json(rows);
  } catch (error) {
    const message = error instanceof Error ? error.message : "Erro interno";
    return NextResponse.json({ error: message }, { status: 500 });
  }
}

export async function POST(request: NextRequest) {
  try {
    const body = await request.json();
    const { nome } = body;

    if (!nome || typeof nome !== "string" || !nome.trim()) {
      return NextResponse.json({ error: "Nome é obrigatório" }, { status: 400 });
    }

    const nomeTrimmed = nome.trim();
    const db = getDb();

    // Check duplicate (case-insensitive)
    const duplicate = db
      .prepare("SELECT id FROM funcionarios WHERE LOWER(nome) = LOWER(?) AND ativo = 1")
      .get(nomeTrimmed) as { id: number } | undefined;

    if (duplicate) {
      return NextResponse.json(
        { error: "Já existe um funcionário com este nome" },
        { status: 409 },
      );
    }

    const result = db
      .prepare("INSERT INTO funcionarios (nome) VALUES (?)")
      .run(nomeTrimmed);

    return NextResponse.json(
      { id: result.lastInsertRowid, nome: nomeTrimmed, ativo: 1 },
      { status: 201 },
    );
  } catch (error) {
    const message = error instanceof Error ? error.message : "Erro interno";
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
```

### API Route — PUT /api/funcionarios/[id]

```typescript
// src/app/api/funcionarios/[id]/route.ts
// Pattern: src/app/api/cil/[cil]/route.ts [VERIFIED: codebase]

import { type NextRequest, NextResponse } from "next/server";
import { getDb } from "@/lib/db";

export async function PUT(
  request: NextRequest,
  { params }: { params: Promise<{ id: string }> },
) {
  try {
    const { id } = await params;
    const funcionarioId = Number(id);
    if (Number.isNaN(funcionarioId)) {
      return NextResponse.json({ error: "ID inválido" }, { status: 400 });
    }

    const body = await request.json();
    const db = getDb();

    // Support partial update: { nome } or { ativo: boolean } or both
    if (body.nome !== undefined) {
      const nome = String(body.nome).trim();
      if (!nome) {
        return NextResponse.json({ error: "Nome é obrigatório" }, { status: 400 });
      }

      // Check duplicate excluindo próprio ID
      const duplicate = db
        .prepare("SELECT id FROM funcionarios WHERE LOWER(nome) = LOWER(?) AND id != ? AND ativo = 1")
        .get(nome, funcionarioId) as { id: number } | undefined;

      if (duplicate) {
        return NextResponse.json(
          { error: "Já existe um funcionário com este nome" },
          { status: 409 },
        );
      }

      db.prepare("UPDATE funcionarios SET nome = ?, updated_at = CURRENT_TIMESTAMP WHERE id = ?")
        .run(nome, funcionarioId);
    }

    if (body.ativo !== undefined) {
      const ativo = body.ativo ? 1 : 0;
      db.prepare("UPDATE funcionarios SET ativo = ?, updated_at = CURRENT_TIMESTAMP WHERE id = ?")
        .run(ativo, funcionarioId);
    }

    const updated = db.prepare("SELECT id, nome, ativo, created_at, updated_at FROM funcionarios WHERE id = ?")
      .get(funcionarioId);

    if (!updated) {
      return NextResponse.json({ error: "Funcionário não encontrado" }, { status: 404 });
    }

    return NextResponse.json(updated);
  } catch (error) {
    const message = error instanceof Error ? error.message : "Erro interno";
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
```

### Migração SQLite

```typescript
// Em src/lib/db.ts, dentro de runMigrations()
// Pattern: migrações existentes [VERIFIED: codebase]

database.exec(`
  CREATE TABLE IF NOT EXISTS funcionarios (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    nome TEXT NOT NULL,
    ativo INTEGER NOT NULL DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
  )
`);
```

### Client Component — Tabela com Busca e Modal

```typescript
// src/app/(main)/dashboard/funcionarios/_components/funcionarios-table.tsx
// Pattern: src/app/(main)/dashboard/kpi/_components/kpi-dashboard-client.tsx [VERIFIED: codebase]
// Pattern: src/app/(main)/dashboard/kpi/_components/carteira-modal.tsx [VERIFIED: codebase]

"use client";

import { useEffect, useState } from "react";

import { Loader2, Pencil, Plus, Search, X } from "lucide-react";

import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";

import { FuncionarioModal } from "./funcionario-modal";

interface Funcionario {
  id: number;
  nome: string;
  ativo: number;
  created_at: string;
  updated_at: string;
}

export function FuncionariosTable() {
  const [funcionarios, setFuncionarios] = useState<Funcionario[]>([]);
  const [loading, setLoading] = useState(true);
  const [search, setSearch] = useState("");
  const [apenasAtivos, setApenasAtivos] = useState(false);
  const [modalOpen, setModalOpen] = useState(false);
  const [editingFuncionario, setEditingFuncionario] = useState<Funcionario | undefined>(undefined);
  const [error, setError] = useState<string | null>(null);

  async function fetchFuncionarios() {
    setLoading(true);
    try {
      const res = await fetch("/api/funcionarios");
      if (!res.ok) throw new Error("Erro ao carregar funcionários");
      const data: Funcionario[] = await res.json();
      setFuncionarios(data);
    } catch (err) {
      setError(err instanceof Error ? err.message : "Erro desconhecido");
    } finally {
      setLoading(false);
    }
  }

  useEffect(() => {
    void fetchFuncionarios();
  }, []);

  // Filter client-side
  const filtered = funcionarios.filter((f) => {
    const matchesSearch = f.nome.toLowerCase().includes(search.toLowerCase());
    const matchesAtivo = !apenasAtivos || f.ativo === 1;
    return matchesSearch && matchesAtivo;
  });

  async function handleToggleAtivo(funcionario: Funcionario) {
    const novoAtivo = funcionario.ativo === 1 ? 0 : 1;
    // Optimistic update
    setFuncionarios((prev) =>
      prev.map((f) => (f.id === funcionario.id ? { ...f, ativo: novoAtivo } : f)),
    );

    try {
      const res = await fetch(`/api/funcionarios/${funcionario.id}`, {
        method: "PUT",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ ativo: novoAtivo === 1 }),
      });
      if (!res.ok) throw new Error("Erro ao atualizar");
    } catch {
      // Revert on error
      setFuncionarios((prev) =>
        prev.map((f) => (f.id === funcionario.id ? { ...f, ativo: funcionario.ativo } : f)),
      );
    }
  }

  function handleAdd() {
    setEditingFuncionario(undefined);
    setModalOpen(true);
  }

  function handleEdit(funcionario: Funcionario) {
    setEditingFuncionario(funcionario);
    setModalOpen(true);
  }

  function handleModalClose() {
    setModalOpen(false);
    setEditingFuncionario(undefined);
  }

  async function handleModalSave(nome: string) {
    const isEdit = editingFuncionario !== undefined;
    const url = isEdit ? `/api/funcionarios/${editingFuncionario.id}` : "/api/funcionarios";
    const method = isEdit ? "PUT" : "POST";

    const res = await fetch(url, {
      method,
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ nome }),
    });

    if (!res.ok) {
      const data = await res.json();
      throw new Error(data.error || "Erro ao salvar");
    }

    void fetchFuncionarios();
    handleModalClose();
  }

  return (
    <>
      <Card>
        <CardHeader>
          <div className="flex items-center justify-between">
            <CardTitle>Todos os Funcionários</CardTitle>
            <Button onClick={handleAdd}>
              <Plus className="size-4" />
              Adicionar Funcionário
            </Button>
          </div>
        </CardHeader>
        <CardContent>
          {/* Search and filter */}
          <div className="mb-4 flex items-center gap-4">
            <div className="relative flex-1">
              <Search className="absolute top-1/2 left-2.5 size-4 -translate-y-1/2 text-muted-foreground" />
              <Input
                placeholder="Buscar por nome..."
                value={search}
                onChange={(e) => setSearch(e.target.value)}
                className="pl-8"
              />
              {search && (
                <button
                  type="button"
                  onClick={() => setSearch("")}
                  className="absolute top-1/2 right-2.5 -translate-y-1/2 text-muted-foreground hover:text-foreground"
                >
                  <X className="size-4" />
                </button>
              )}
            </div>
            <label className="flex items-center gap-2 text-sm">
              <input
                type="checkbox"
                checked={apenasAtivos}
                onChange={(e) => setApenasAtivos(e.target.checked)}
                className="size-4"
              />
              Só ativos
            </label>
          </div>

          {loading ? (
            <div className="flex items-center justify-center py-12">
              <Loader2 className="size-6 animate-spin text-muted-foreground" />
            </div>
          ) : error ? (
            <div className="rounded-lg border border-destructive/50 bg-destructive/5 p-4 text-destructive text-sm">
              {error}
            </div>
          ) : (
            <Table>
              <TableHeader>
                <TableRow>
                  <TableHead>Nome</TableHead>
                  <TableHead>Status</TableHead>
                  <TableHead className="text-right">Ações</TableHead>
                </TableRow>
              </TableHeader>
              <TableBody>
                {filtered.length === 0 ? (
                  <TableRow>
                    <TableCell colSpan={3} className="text-center text-muted-foreground">
                      Nenhum funcionário encontrado
                    </TableCell>
                  </TableRow>
                ) : (
                  filtered.map((f) => (
                    <TableRow key={f.id}>
                      <TableCell className="font-medium">{f.nome}</TableCell>
                      <TableCell>
                        <Badge variant={f.ativo === 1 ? "default" : "secondary"}>
                          {f.ativo === 1 ? "Ativo" : "Inativo"}
                        </Badge>
                      </TableCell>
                      <TableCell className="text-right">
                        <div className="flex items-center justify-end gap-2">
                          <Button variant="ghost" size="icon-sm" onClick={() => handleEdit(f)} title="Editar">
                            <Pencil className="size-4" />
                          </Button>
                          <Button
                            variant="outline"
                            size="xs"
                            onClick={() => handleToggleAtivo(f)}
                          >
                            {f.ativo === 1 ? "Desativar" : "Ativar"}
                          </Button>
                        </div>
                      </TableCell>
                    </TableRow>
                  ))
                )}
              </TableBody>
            </Table>
          )}
        </CardContent>
      </Card>

      {modalOpen && (
        <FuncionarioModal
          open={modalOpen}
          funcionario={editingFuncionario}
          onSave={handleModalSave}
          onClose={handleModalClose}
        />
      )}
    </>
  );
}
```

### Modal Reutilizável — Add / Edit

```typescript
// src/app/(main)/dashboard/funcionarios/_components/funcionario-modal.tsx
// Pattern: src/app/(main)/dashboard/kpi/_components/carteira-modal.tsx [VERIFIED: codebase]

"use client";

import { useEffect, useState } from "react";

import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";

interface FuncionarioModalProps {
  open: boolean;
  funcionario?: { id: number; nome: string };
  onSave: (nome: string) => Promise<void>;
  onClose: () => void;
}

export function FuncionarioModal({ open, funcionario, onSave, onClose }: FuncionarioModalProps) {
  const [nome, setNome] = useState("");
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState<string | null>(null);

  const isEdit = funcionario !== undefined;

  useEffect(() => {
    if (open) {
      setNome(funcionario?.nome ?? "");
      setError(null);
    }
  }, [open, funcionario]);

  async function handleSave() {
    if (!nome.trim()) {
      setError("O nome é obrigatório");
      return;
    }

    setSaving(true);
    setError(null);

    try {
      await onSave(nome.trim());
    } catch (err) {
      setError(err instanceof Error ? err.message : "Erro ao salvar");
    } finally {
      setSaving(false);
    }
  }

  return (
    <Dialog
      open={open}
      onOpenChange={(open) => {
        if (!open) onClose();
      }}
    >
      <DialogContent>
        <DialogHeader>
          <DialogTitle>{isEdit ? "Editar Funcionário" : "Adicionar Funcionário"}</DialogTitle>
          <DialogDescription>
            {isEdit ? "Altere o nome do funcionário" : "Insira o nome do novo funcionário"}
          </DialogDescription>
        </DialogHeader>

        <div className="flex flex-col gap-4 py-2">
          <div className="flex flex-col gap-1">
            <label className="text-muted-foreground text-sm" htmlFor="nome-input">
              Nome
            </label>
            <Input
              id="nome-input"
              value={nome}
              onChange={(e) => setNome(e.target.value)}
              placeholder="Nome do funcionário"
              onKeyDown={(e) => {
                if (e.key === "Enter") void handleSave();
              }}
            />
          </div>

          {error && <p className="text-destructive text-xs">{error}</p>}

          <div className="flex justify-end gap-2">
            <Button variant="outline" onClick={onClose} disabled={saving}>
              Cancelar
            </Button>
            <Button onClick={handleSave} disabled={saving || !nome.trim()}>
              {saving ? "Guardando..." : "Guardar"}
            </Button>
          </div>
        </div>
      </DialogContent>
    </Dialog>
  );
}
```

## State of the Art

| Old Approach | Current Approach | When Changed | Impact |
|--------------|------------------|--------------|--------|
| — | Create/Edit Modal reuse pattern | — | Único componente `funcionario-modal.tsx` serve para add e edit, diferenciado pela presença de `funcionario` prop |

**Nesta fase não há nada deprecated — todos os patterns são os estabelecidos nas Fases 1-3.**

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A1 | O sidebar item "Funcionários" já existe e não precisa ser criado | Architecture Patterns | Baixo — se o item não existir ou estiver como `comingSoon: true`, o link não funciona até corrigir |

**Nota:** A1 foi verificado no código — o item existe em `sidebar-items.ts` linha 62-65 com `url: "/dashboard/funcionarios"`, `icon: Users`, sem `comingSoon`. [VERIFIED: codebase]

## Open Questions

1. **Badge variant para "Inativo"?**
   - O que sabemos: shadcn Badge tem variants `default`, `secondary`, `destructive`, `outline`, `ghost`, `link`. Não há `success`.
   - O que está em aberto: qual variant usar para o status "Inativo" — `secondary` (cinza) é a escolha natural.
   - Recomendação: `default` para Ativo (verde/padrão), `secondary` para Inativo (cinza). Ambos são variants válidas.

2. **Ordenação da tabela?**
   - O que sabemos: decisão D4.1-04 diz colunas Nome, Status, Ações.
   - O que está em aberto: ordenar por nome alfabeticamente? Query SQL `ORDER BY nome COLLATE NOCASE` resolve.
   - Recomendação: ordenar por nome ASC no backend; filtragem adicional client-side é opcional.

## Environment Availability

> Para esta fase puramente code-level (API routes + UI components + DB migration + sidebar item já existente), nenhuma dependência externa nova é necessária. Todas as ferramentas (Next.js, TypeScript, Biome, better-sqlite3, shadcn/ui) já estão instaladas e configuradas.

**Step 2.6: SKIPPED (no external dependencies identified — all stack components already installed and configured)**

## Validation Architecture

> `workflow.nyquist_validation` is enabled in `.planning/config.json` (value: true). This section is included.

### Test Framework

| Property | Value |
|----------|-------|
| Framework | Nenhum encontrado no projeto |
| Config file | — |
| Quick run command | N/A — projeto não tem testes |
| Full suite command | N/A — projeto não tem testes |

**Status:** O projeto atualmente não possui infraestrutura de testes configurada (sem Jest, Vitest, Playwright ou qualquer test runner). A Fase 4.1 é CRUD-only com validação de backend. Testes manuais via navegação são o método de verificação atual.

### Phase Requirements → Test Map

| Req ID | Behavior | Test Type | Automated Command | File Exists? |
|--------|----------|-----------|-------------------|-------------|
| SC-01 | Listar funcionários | manual | N/A | ❌ |
| SC-02 | Adicionar funcionário via modal | manual | N/A | ❌ |
| SC-03 | Toggle ativo/inativo | manual | N/A | ❌ |
| SC-04 | Migração tabela SQLite | manual | N/A | ❌ |
| SC-05 | API endpoints respondem | manual | N/A | ❌ |

### Wave 0 Gaps

- [ ] `src/__tests__/` — nenhum diretório de testes encontrado
- [ ] Test framework — nenhum instalado (Jest, Vitest, Playwright ausentes)

*(Nota: Embora a validação Nyquist esteja habilitada, o projeto não possui infraestrutura de testes. Esta fase implementa CRUD que pode ser verificado manualmente via navegação. A adição de testes está fora do escopo desta fase.)*

## Security Domain

> `security_enforcement` is not explicitly set to false, so it's treated as enabled.

### Applicable ASVS Categories

| ASVS Category | Applies | Standard Control |
|---------------|---------|-----------------|
| V2 Authentication | no | Sem autenticação — dashboard local |
| V3 Session Management | no | Sem sessão de usuário |
| V4 Access Control | no | Sem multiusuário |
| V5 Input Validation | yes | Validação de `nome` no backend (trim, tipo, obrigatório); validação no endpoint PUT para campos parciais |
| V6 Cryptography | no | Sem dados sensíveis armazenados |

### Known Threat Patterns

| Pattern | STRIDE | Standard Mitigation |
|---------|--------|---------------------|
| SQL Injection in nome field | Tampering | better-sqlite3 prepared statements (parametrização: `?` placeholders) — já mitigado pelo pattern existente |
| Mass assignment via PUT | Tampering | Validar explicitamente campos esperados (`nome` e/ou `ativo`), ignorar campos extras no body |
| Integer overflow in ID param | Tampering | Validar `Number(id)` com `Number.isNaN()` — ver código exemplo |
| Duplicate name bypass (case) | Tampering | Usar `LOWER()` em todas as verificações de duplicado |

**Nota:** Como o projeto é um dashboard local (single-user, sem autenticação), o vetor de ataque principal é a manipulação maliciosa via chamadas API diretas. Prepared statements e validação explícita de campos mitigam os riscos.

## Sources

### Primary (HIGH confidence)
- `src/app/(main)/dashboard/kpi/page.tsx` — Server component page pattern [VERIFIED: codebase]
- `src/app/(main)/dashboard/kpi/_components/kpi-dashboard-client.tsx` — Client component com fetch/loading/error [VERIFIED: codebase]
- `src/app/(main)/dashboard/kpi/_components/carteira-modal.tsx` — Modal Dialog pattern [VERIFIED: codebase]
- `src/app/api/carteira/route.ts` — API route pattern [VERIFIED: codebase]
- `src/app/api/cil/[cil]/route.ts` — Dynamic route pattern [VERIFIED: codebase]
- `src/lib/db.ts` — SQLite singleton + migration pattern [VERIFIED: codebase]
- `src/navigation/sidebar/sidebar-items.ts` — Sidebar item já existe [VERIFIED: codebase]
- `src/components/ui/dialog.tsx` — Dialog component [VERIFIED: codebase]
- `src/components/ui/table.tsx` — Table component [VERIFIED: codebase]
- `src/components/ui/badge.tsx` — Badge component [VERIFIED: codebase]
- `biome.json` — Lint rules (useFilenamingConvention, noFloatingPromises, etc.) [VERIFIED: codebase]

## Metadata

**Confidence breakdown:**
- Standard stack: HIGH — todos os componentes já existem no projeto, nenhuma instalação nova necessária
- Architecture: HIGH — patterns consistentes das Fases 1-3; sidebar item já existe
- Pitfalls: HIGH — todos identificados na base de código existente (SQLite case-sensitivity, Biome rules, modal state reset)

**Research date:** 2026-06-07
**Valid until:** 2026-07-07 (padrões estáveis — Next.js 16 + shadcn/ui v4)

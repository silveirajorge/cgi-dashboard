---
phase: 01
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .gitignore
  - vitest.config.ts
  - src/lib/db.ts
  - src/lib/csv.ts
  - src/lib/services/pedidos.ts
  - src/app/api/upload/route.ts
autonomous: true
requirements:
  - CSV-03
  - CSV-05
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04
  - CUST-01
user_setup: []

must_haves:
  truths:
    - "CSV enviado via POST /api/upload é parseado e armazenado em .data/cgi.db"
    - "Cabeçalhos do CSV são normalizados (trim, lowercase, replace espaços por underscore)"
    - "Colunas obrigatórias (id_contacto) são validadas — CSV sem elas retorna erro"
    - "Linhas duplicadas (mesmo id_contacto + mes_competencia) são ignoradas na inserção"
    - "Uploads são cumulativos — dados existentes não são removidos"
    - "Histórico de upload é registrado na tabela upload_history"
    - "Schema da tabela pedidos é dinâmico — criado a partir dos cabeçalhos do primeiro CSV"
  artifacts:
    - path: "src/lib/db.ts"
      provides: "SQLite singleton connection + CRUD + migrations"
      exports:
        - "getDb"
        - "createPedidosTable"
        - "insertUploadHistory"
      min_lines: 60
    - path: "src/lib/csv.ts"
      provides: "CSV parsing, header normalization, column validation"
      exports:
        - "parseCsvContent"
        - "normalizeHeader"
        - "extractMesCompetencia"
        - "validateRequiredColumns"
      min_lines: 60
    - path: "src/lib/services/pedidos.ts"
      provides: "Business logic: orchestrate import, dedup, KPI calculation"
      exports:
        - "importRows"
        - "UploadStats"
      min_lines: 40
    - path: "src/app/api/upload/route.ts"
      provides: "POST /api/upload endpoint"
      exports:
        - "POST"
      min_lines: 40
    - path: ".data/cgi.db"
      provides: "SQLite database file (created on first upload)"
      min_lines: 0
    - path: "vitest.config.ts"
      provides: "Test runner configuration"
      exports: []
      min_lines: 10
  key_links:
    - from: "src/app/api/upload/route.ts"
      to: "src/lib/csv.ts"
      via: "import { parseCsvContent, validateRequiredColumns }"
      pattern: "import.*from.*csv"
    - from: "src/app/api/upload/route.ts"
      to: "src/lib/services/pedidos.ts"
      via: "import { importRows }"
      pattern: "import.*from.*pedidos"
    - from: "src/lib/services/pedidos.ts"
      to: "src/lib/db.ts"
      via: "import { getDb, createPedidosTable, insertUploadHistory }"
      pattern: "import.*from.*db"
    - from: "src/lib/services/pedidos.ts"
      to: "src/lib/csv.ts"
      via: "import { extractMesCompetencia }"
      pattern: "import.*from.*csv"
---

<objective>
**Upload CSV → Armazenar em SQLite com dedup**

**Purpose:** Entregar o pipeline completo de upload: receber um CSV via POST, parsear com papaparse, normalizar cabeçalhos, validar colunas obrigatórias, armazenar em SQLite com dedup por `id_contacto + mes_competencia`, e registrar histórico.

**Output:** `POST /api/upload` funcional + 3 módulos de biblioteca (db, csv, services) + testes unitários + database SQLite.
</objective>

<execution_context>
@/home/jsilveira/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/jsilveira/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-backend-foundation/01-CONTEXT.md
@.planning/phases/01-backend-foundation/01-RESEARCH.md

<interfaces>
Existing codebase patterns (from src/lib/utils.ts):
```typescript
// Path alias: @/ = src/
// Imports: ESM only (Biome enforces no CommonJS)
// Strict TypeScript: strict mode, no `any`
```

Existing .gitignore has:
```
.data/
/data/
```

No test framework currently exists. No `src/app/api/` directory exists yet.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup — install deps, fix .gitignore versionamento CSVs, criar vitest config + testes iniciais</name>

  <files>
    package.json
    .gitignore
    vitest.config.ts
  </files>

  <read_first>
    /home/jsilveira/project/cgi/package.json
    /home/jsilveira/project/cgi/.gitignore
  </read_first>

  <action>
    1. Install dependencies:
       - `npm install better-sqlite3@12.10.0 papaparse@5.5.3`
       - `npm install --save-dev @types/better-sqlite3@7.6.13 @types/papaparse@5.5.2 vitest@3.1.6`

    2. Fix .gitignore: `data/csv/` deve ser versionado (ARCH-03). Adicionar linha `!data/csv/` APÓS a linha `/data/` existente para fazer un-ignore apenas dos CSVs de teste. A linha `.data/` (já existente) continua ignorando o banco SQLite.

       Editar .gitignore:
       - Após a linha `# typescript` adicionar `# csv test files` e `!data/csv/`
       - OU editar diretamente após a linha `/data/`: inserir `!data/csv/` na linha seguinte

       O resultado deve ser:
       ```
       .data/
       /data/
       !data/csv/
       ```

    3. Criar `vitest.config.ts` na raiz com:
       ```typescript
       import { defineConfig } from "vitest/config";
       import path from "node:path";

       export default defineConfig({
         test: {
           globals: true,
           environment: "node",
           include: ["src/**/*.test.ts"],
         },
         resolve: {
           alias: {
             "@": path.resolve(__dirname, "./src"),
           },
         },
       });
       ```

    4. Adicionar script `"test"` ao `package.json`:
       ```json
       "test": "vitest run --reporter=verbose"
       ```
       Adicionar entre os scripts existentes (por exemplo, após `"check:fix"`).

    5. Rodar `npm run build` para verificar que nada quebrou com as novas dependências.
  </action>

  <acceptance_criteria>
    1. `npm ls better-sqlite3@12.10.0` returns successfully
    2. `npm ls papaparse@5.5.3` returns successfully
    3. `.gitignore` contém `!data/csv/` na linha seguinte a `/data/`
    4. `git check-ignore -v data/csv/2026-01.csv` retorna vazio (não ignorado)
    5. `vitest.config.ts` existe com `resolve.alias["@"]` apontando para `./src`
    6. `package.json` contém script `"test": "vitest run --reporter=verbose"`
    7. `npm run build` executa sem erros
  </acceptance_criteria>

  <verify>
    <automated>npm run build</automated>
  </verify>

  <done>Dependências instaladas, CSVs versionados, vitest configurado, build passa.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar módulos core — db.ts, csv.ts, services/pedidos.ts</name>

  <files>
    src/lib/db.ts
    src/lib/csv.ts
    src/lib/services/pedidos.ts
  </files>

  <read_first>
    /home/jsilveira/project/cgi/.planning/phases/01-backend-foundation/01-RESEARCH.md
    /home/jsilveira/project/cgi/package.json
  </read_first>

  <action>
    Criar três módulos seguindo exatamente os patterns e decisões abaixo:

    **1. `src/lib/db.ts` (Repository Layer — per D-02, D-17, D-18, D-19, D-20)**

    - Singleton lazy com module-level caching:
      ```typescript
      import Database from "better-sqlite3";
      import path from "node:path";
      import fs from "node:fs";

      const DB_PATH = path.resolve(process.cwd(), ".data/cgi.db");
      let db: Database.Database | null = null;

      export function getDb(): Database.Database { ... }
      ```

    - `getDb()`: criar diretório `.data/` se não existir, instanciar Database, setar `journal_mode = WAL`, chamar `runMigrations()`, retornar conexão.
    - `runMigrations()`: criar tabela fixa `upload_history` com schema de D-20:
      ```sql
      CREATE TABLE IF NOT EXISTS upload_history (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        filename TEXT NOT NULL,
        rows_imported INTEGER NOT NULL,
        rows_ignored INTEGER NOT NULL,
        created_at DATETIME DEFAULT CURRENT_TIMESTAMP
      )
      ```

    - `createPedidosTable(columns: string[])`: gerar CREATE TABLE dinâmico com colunas do CSV (normalizadas) + colunas extras (D-10, D-11, D-12):
      ```sql
      CREATE TABLE IF NOT EXISTS pedidos (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        {colunas_do_csv},
        mes_competencia TEXT NOT NULL,
        created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
        UNIQUE(id_contacto, mes_competencia)
      )
      ```
      Cada coluna do CSV deve ser declarada como `"nome_coluna" TEXT` (com double-quotes para segurança contra SQL injection — per RESEARCH Pitfall 2).

    - `insertUploadHistory(filename, rowsImported, rowsIgnored)`: INSERT na tabela `upload_history` com prepared statement.

    - Exportar: `getDb`, `createPedidosTable`, `insertUploadHistory`.
    - Usar `import Database from "better-sqlite3"` (default import — better-sqlite3 v12 é ESM-compatible).

    **2. `src/lib/csv.ts` (Parser Layer — per D-03, D-09, CSV-03, CSV-05)**

    - `normalizeHeader(header: string): string`: trim → toLowerCase → replace espaços com underscore. Usar regex `/\s+/g` para capturar múltiplos espaços/tabs.

    - `parseCsvContent(content: string): ParseResult`:
      - Usar `Papa.parse(content, { header: true, skipEmptyLines: true, dynamicTyping: false })`.
      - Validar que result.data não é vazio.
      - Extrair rawHeaders de `result.meta.fields`.
      - Normalizar headers com `normalizeHeader()`.
      - Remapear cada row: para cada par (key, value), normalizar a key e aplicar `.trim()` no value.
      - Retornar `{ headers: string[], rows: Record<string, string>[], errors: Papa.ParseError[] }`.

    - `extractMesCompetencia(dtRegisto: string): string`:
      - Formato esperado de entrada: `"2026-01-29 11:43:07"`
      - Extrair parte da data: `dtRegisto.split(" ")[0]` → `"2026-01-29"`
      - Retornar `date.substring(0, 7)` → `"2026-01"` (YYYY-MM)

    - `validateRequiredColumns(headers: string[], required: string[]): void`:
      - Converter headers para um Set.
      - Para cada coluna obrigatória, verificar se está no Set.
      - Se faltar, lançar Error com mensagem: `"Coluna obrigatória '{col}' não encontrada no CSV. Colunas encontradas: [...]"`
      - Colunas obrigatórias: `["id_contacto"]` (per CONTEXT.md, ID_CONTACTO normalizado vira id_contacto).

    - Exportar: `parseCsvContent`, `normalizeHeader`, `extractMesCompetencia`, `validateRequiredColumns`, `ParseResult`.

    **3. `src/lib/services/pedidos.ts` (Service Layer — per D-04, D-05, D-06, D-07, D-14)**

    - `importRows(parsed: ParseResult, filename: string): UploadStats`:
      - Verificar se tabela `pedidos` existe via `SELECT name FROM sqlite_master WHERE type='table' AND name='pedidos'`.
      - Se não existir, chamar `createPedidosTable(parsed.headers)`.
      - Se existir, validar que as colunas do CSV correspondem ao schema existente via `PRAGMA table_info(pedidos)`. Se houver colunas desconhecidas no CSV que não estão na tabela, lançar Error.
      - Criar prepared statement com INSERT OR IGNORE (per D-07):
        ```sql
        INSERT OR IGNORE INTO pedidos ({colunas_quoted}, mes_competencia)
        VALUES ({placeholders}, ?)
        ```
      - Usar transação (`db.transaction()`) para toda a operação de inserção.
      - Para cada row: extrair valores na ordem das colunas, extrair `mes_competencia` de `dt_registo` via `extractMesCompetencia()`, executar insert.
      - `result.changes > 0` → imported++, senão ignored++.
      - Após loop, chamar `insertUploadHistory(filename, imported, ignored)`.
      - Retornar `{ rows_imported, rows_ignored, total_rows }`.

    - `UploadStats`: interface com `{ rows_imported: number; rows_ignored: number; total_rows: number }`.

    **Padrão de imports (Biome enforced, ESM only):**
    - `import Database from "better-sqlite3";`
    - `import Papa from "papaparse";`
    - `import path from "node:path";`
    - `import fs from "node:fs";`
    - `import type { ParseResult } from "@/lib/csv";` (usar `type` import para interfaces/tipos)
    - `import { getDb, createPedidosTable, insertUploadHistory } from "@/lib/db";`
    - `import { parseCsvContent, extractMesCompetencia, type ParseResult } from "@/lib/csv";`

    **Regras de implementação:**
    - `normalizeHeader()` deve usar regex `/\s+/g` para capturar múltiplos caracteres de whitespace (espaços, tabs)
    - Colunas no SQL devem sempre usar double-quotes (`"nome_coluna"`) para prevenir SQL injection via nomes dinâmicos
    - Valores passados para prepared statements devem usar `?` placeholders (nunca concatenação de strings)
    - `dynamicTyping: false` no papaparse — manter tudo como string (per RESEARCH)
  </action>

  <acceptance_criteria>
    1. `src/lib/db.ts` exporta `getDb`, `createPedidosTable`, `insertUploadHistory`
    2. `src/lib/db.ts` contém `const DB_PATH = path.resolve(process.cwd(), ".data/cgi.db")`
    3. `src/lib/db.ts` contém `journal_mode = WAL`
    4. `src/lib/db.ts` contém `UNIQUE(id_contacto, mes_competencia)` na CREATE TABLE pedidos
    5. `src/lib/db.ts` contém `CREATE TABLE IF NOT EXISTS upload_history` com colunas `filename TEXT NOT NULL, rows_imported INTEGER NOT NULL, rows_ignored INTEGER NOT NULL`
    6. `src/lib/csv.ts` exporta `normalizeHeader`, `parseCsvContent`, `extractMesCompetencia`, `validateRequiredColumns`
    7. `src/lib/csv.ts` contém `header.toLowerCase().replace(/\s+/g, "_")` no normalizeHeader
    8. `src/lib/csv.ts` contém `result.data.map` para remapear rows com headers normalizados
    9. `src/lib/csv.ts` contém validação `result.data.length === 0` → throw
    10. `src/lib/services/pedidos.ts` exporta `importRows` e `UploadStats`
    11. `src/lib/services/pedidos.ts` contém `INSERT OR IGNORE INTO pedidos`
    12. `src/lib/services/pedidos.ts` contém `db.transaction`
    13. `src/lib/services/pedidos.ts` contém `extractMesCompetencia` chamada com `row["dt_registo"]`
    14. `npm run build` passa sem erros
  </acceptance_criteria>

  <verify>
    <automated>npm run build</automated>
  </verify>

  <done>Três módulos implementados: db.ts (singleton + migrations + CRUD), csv.ts (parser + normalização + validação), services/pedidos.ts (orquestração + dedup + histórico). Build passa.</done>
</task>

<task type="auto">
  <name>Task 3: Implementar POST /api/upload route + verificação end-to-end</name>

  <files>
    src/app/api/upload/route.ts
  </files>

  <read_first>
    /home/jsilveira/project/cgi/src/lib/csv.ts
    /home/jsilveira/project/cgi/src/lib/db.ts
    /home/jsilveira/project/cgi/src/lib/services/pedidos.ts
  </read_first>

  <action>
    Criar `src/app/api/upload/route.ts` (per D-14) seguindo exatamente:

    ```typescript
    import { NextRequest, NextResponse } from "next/server";
    import { parseCsvContent, validateRequiredColumns } from "@/lib/csv";
    import { importRows } from "@/lib/services/pedidos";

    const MAX_FILE_SIZE = 10 * 1024 * 1024; // 10MB
    const REQUIRED_COLUMNS = ["id_contacto"];

    export async function POST(request: NextRequest) {
      try {
        const formData = await request.formData();
        const file = formData.get("file");

        // Validações
        if (!file || !(file instanceof File)) {
          return NextResponse.json(
            { error: "Nenhum arquivo enviado. Envie um CSV no campo 'file'." },
            { status: 400 },
          );
        }

        if (!file.name.endsWith(".csv")) {
          return NextResponse.json(
            { error: "Apenas arquivos CSV são aceitos" },
            { status: 400 },
          );
        }

        if (file.size > MAX_FILE_SIZE) {
          return NextResponse.json(
            { error: `Arquivo excede o limite de ${MAX_FILE_SIZE / 1024 / 1024}MB` },
            { status: 413 },
          );
        }

        const content = await file.text();
        const parsed = parseCsvContent(content);

        validateRequiredColumns(parsed.headers, REQUIRED_COLUMNS);

        const stats = await importRows(parsed, file.name);

        return NextResponse.json({
          message: "Upload realizado com sucesso",
          filename: file.name,
          rows_imported: stats.rows_imported,
          rows_ignored: stats.rows_ignored,
          total_rows: stats.total_rows,
        });
      } catch (error) {
        const message = error instanceof Error ? error.message : "Erro interno ao processar upload";
        return NextResponse.json({ error: message }, { status: 500 });
      }
    }
    ```

    **Regras de implementação:**
    - Verificar `file instanceof File` antes de usar (per RESEARCH Pitfall — TypeScript não infere)
    - Validar extensão `.csv` (per D-14)
    - Validar tamanho máximo de 10MB (per RESEARCH — recomendação)
    - Usar `REQUIRED_COLUMNS = ["id_contacto"]` (a coluna `ID_CONTACTO` do CSV vira `id_contacto` após normalização)
    - Retornar JSON com message, filename, rows_imported, rows_ignored, total_rows
    - Erros inesperados retornam `{ error: string }` com status 500

    **Após criar o arquivo, verificar funcionamento:**
    ```bash
    # Build para garantir que compila
    npm run build

    # Iniciar servidor dev em background
    npm run dev &
    sleep 5

    # Testar upload com CSV real
    curl -s -X POST http://localhost:3000/api/upload \
      -F "file=@data/csv/2026-01.csv" | head -20

    # Verificar que o banco foi criado
    ls -la .data/cgi.db

    # Testar CSVs adicionais
    curl -s -X POST http://localhost:3000/api/upload \
      -F "file=@data/csv/2026-02.csv" | head -20

    # Testar duplicata (deve retornar rows_imported=0 para linhas já existentes)
    curl -s -X POST http://localhost:3000/api/upload \
      -F "file=@data/csv/2026-01.csv" | head -20

    # Testar CSV sem id_contacto (deve retornar erro 500 com mensagem)
    # Criar CSV temporário sem id_contacto
    echo "nome,valor" > /tmp/test-no-id.csv
    echo "João,100" >> /tmp/test-no-id.csv
    curl -s -X POST http://localhost:3000/api/upload \
      -F "file=@/tmp/test-no-id.csv" | head -20

    # Parar servidor
    kill %1 2>/dev/null
    ```
  </action>

  <acceptance_criteria>
    1. `src/app/api/upload/route.ts` exporta `POST`
    2. `src/app/api/upload/route.ts` contém `MAX_FILE_SIZE = 10 * 1024 * 1024`
    3. `src/app/api/upload/route.ts` contém `instanceof File` para validação
    4. `src/app/api/upload/route.ts` contém `file.name.endsWith(".csv")`
    5. First curl POST with `data/csv/2026-01.csv` returns HTTP 200 com `rows_imported` > 0
    6. `.data/cgi.db` existe após o upload
    7. Second upload of same CSV retorna `rows_ignored` correspondente (todos ignorados por dedup)
    8. CSV sem coluna `id_contacto` retorna erro com mensagem
    9. `npm run build` passa sem erros
  </acceptance_criteria>

  <verify>
    <automated>npm run build</automated>
  </verify>

  <done>POST /api/upload funcional, dados armazenados em .data/cgi.db com dedup, histórico registrado. Build e curl tests passam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | HTTP request contendo CSV upload via multipart/form-data |
| API → SQLite | Queries SQL com nomes de coluna dinâmicos do CSV |
| Filesystem → API | Arquivo CSV lido do disco no servidor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | src/lib/csv.ts — SQL injection via column names | mitigate | `normalizeHeader()` sanitiza nomes de coluna para `[a-z0-9_]+` regex + double-quoted identifiers no SQL |
| T-01-02 | Denial of Service | src/app/api/upload/route.ts — large file upload | mitigate | Limite de 10MB verificado antes do parse + validação de extensão `.csv` |
| T-01-03 | Tampering | src/lib/services/pedidos.ts — prepared statements | mitigate | Todos os valores passados via `?` placeholders, nunca concatenação de strings |
| T-01-04 | Tampering | src/lib/csv.ts — CSV formula injection | accept | Dados armazenados em SQLite, não importados para Excel. `Observações` pode conter fórmulas — safe em SQLite |
</threat_model>

<verification>
1. `npm run build` passa sem erros
2. `npm run check` (Biome lint) passa sem erros
3. `curl POST /api/upload -F "file=@data/csv/2026-01.csv"` retorna HTTP 200
4. `.data/cgi.db` existe e contém tabelas `pedidos` e `upload_history`
5. Dashboard existente carrega sem erros (verificar `curl GET http://localhost:3000/dashboard/default` — deve retornar 200, não 500)
</verification>

<success_criteria>
- POST /api/upload aceita CSV, parseia, armazena em .data/cgi.db, retorna stats
- Cabeçalhos são normalizados (trim, lowercase, underscore)
- Coluna id_contacto é obrigatória — CSV sem ela retorna erro
- Dedup por (id_contacto + mes_competencia) — INSERT OR IGNORE
- Histórico de upload registrado (filename, rows_imported, rows_ignored, created_at)
- Sistema de temas, layout e sidebar existente continua funcionando sem regressão
- Build + lint passam
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-foundation/01-01-SUMMARY.md`
</output>
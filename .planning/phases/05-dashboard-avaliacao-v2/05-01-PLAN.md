---
phase: 05-dashboard-avaliacao-v2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/avaliacoes/dashboard/route.ts
  - src/app/api/avaliacoes/semanas/route.ts
  - src/app/api/avaliacoes/funcionario/[id]/historico/route.ts
  - src/lib/carry-forward.ts
  - src/app/(main)/dashboard/avaliacao-dashboard/_components/avaliacao-dashboard-v2-client.tsx
  - src/app/(main)/dashboard/avaliacao-dashboard/_components/kpi-cards-v2.tsx
  - src/app/(main)/dashboard/avaliacao-dashboard/_components/tendencia-indicator.tsx
  - src/app/(main)/dashboard/avaliacao-dashboard/_components/distribuicao-donut.tsx
  - src/app/(main)/dashboard/avaliacao-dashboard/_components/ferramenta-bar.tsx
  - src/app/(main)/dashboard/avaliacao-dashboard/_components/evolucao-prod-chart.tsx
  - src/app/(main)/dashboard/avaliacao-dashboard/_components/equipa-table.tsx
  - src/app/(main)/dashboard/avaliacao-dashboard/_components/star-rating.tsx
  - src/app/(main)/dashboard/avaliacao-dashboard/_components/score-badge.tsx
  - src/app/(main)/dashboard/avaliacao-dashboard/_components/resumo-card.tsx
  - src/app/(main)/dashboard/avaliacao-dashboard/_components/score-evol-chart.tsx
  - src/app/(main)/dashboard/avaliacao-dashboard/_components/prod-hist-chart.tsx
  - src/app/(main)/dashboard/avaliacao-dashboard/_components/comentarios-list.tsx
  - src/app/(main)/dashboard/avaliacao-dashboard/_components/funcionario-select.tsx
  - src/app/(main)/dashboard/avaliacao-dashboard/page.tsx
autonomous: true
requirements:
  - D5-01
  - D5-02
  - D5-03
  - D5-04
  - D5-05
  - D5-06
  - D5-07
  - D5-08
  - D5-09
  - D5-10
  - D5-11
  - D5-12
  - D5-13
  - D5-14
  - D5-15
  - D5-16
  - D5-17
  - D5-18
  - D5-19
  - D5-20
  - D5-21
  - D5-22

must_haves:
  truths:
    - "Usuário vê 6 cards KPI da equipa (Produtividade Média, Uso Speedops, Erros Críticos, Atrasos/Faltas, Média Auditor, Média Supervisor) com indicador de tendência ▲/▼/→ comparando semana atual com anterior"
    - "Usuário vê gráfico donut 'Distribuição de Desempenho' com contagem por categoria (Excelente/Bom/Atenção/Crítico) e total no centro"
    - "Usuário vê barra horizontal 'Uso da Ferramenta' com % utilizou (azul) vs não utilizou (vermelho) e legenda"
    - "Usuário vê gráfico de linha 'Evolução Média de Produtividade' com área gradiente semanas"
    - "Usuário vê tabela da equipa com: Nome, Atrasos/Faltas, Speedops, Erro Crítico, Produtividade (%), Avaliação Auditor (valor + estrelas), Avaliação Supervisor (valor + estrelas), Score Final, Status (badge colorido)"
    - "Usuário pode selecionar funcionário no dropdown e ver: Score Final grande com medalha, status, gráfico evolução do score (linha gradiente), gráfico histórico de produtividade (barras), e card de comentários"
    - "Dados são filtrados por período (from/to) e exibidos semana a semana, com carry forward de semanas sem avaliações"
  artifacts:
    - path: "src/app/api/avaliacoes/dashboard/route.ts"
      provides: "Dashboard API — KPIs agregados + comparativo semanal + Score Final equipa + distribuição + uso ferramenta"
      min_lines: 90
    - path: "src/app/api/avaliacoes/semanas/route.ts"
      provides: "Semanas disponíveis no banco"
      min_lines: 20
    - path: "src/app/api/avaliacoes/funcionario/[id]/historico/route.ts"
      provides: "Histórico individual: evolution + comentários"
      min_lines: 60
    - path: "src/lib/carry-forward.ts"
      provides: "Função fillCarryForward — preenche gaps de semanas sem dados"
      exports: ["fillCarryForward"]
    - path: "src/app/(main)/dashboard/avaliacao-dashboard/_components/avaliacao-dashboard-v2-client.tsx"
      provides: "Client component principal — fetch, estado, layout 2 colunas (65/35), coordena all sub-components"
      min_lines: 180
    - path: "src/app/(main)/dashboard/avaliacao-dashboard/_components/kpi-cards-v2.tsx"
      provides: "6 cards KPI com indicador de tendência (D5-06, D5-07)"
    - path: "src/app/(main)/dashboard/avaliacao-dashboard/_components/distribuicao-donut.tsx"
      provides: "PieChart donut com label central (D5-08)"
    - path: "src/app/(main)/dashboard/avaliacao-dashboard/_components/ferramenta-bar.tsx"
      provides: "Barra horizontal uso ferramenta (D5-09)"
    - path: "src/app/(main)/dashboard/avaliacao-dashboard/_components/evolucao-prod-chart.tsx"
      provides: "LineChart com gradiente (D5-10)"
    - path: "src/app/(main)/dashboard/avaliacao-dashboard/_components/equipa-table.tsx"
      provides: "Tabela da equipa com StarRating + ScoreBadge (D5-11)"
    - path: "src/app/(main)/dashboard/avaliacao-dashboard/_components/resumo-card.tsx"
      provides: "Card resumo individual (D5-13)"
    - path: "src/app/(main)/dashboard/avaliacao-dashboard/_components/score-evol-chart.tsx"
      provides: "LineChart score individual com gradiente (D5-14)"
    - path: "src/app/(main)/dashboard/avaliacao-dashboard/_components/comentarios-list.tsx"
      provides: "Lista de comentários (D5-16)"
    - path: "src/app/(main)/dashboard/avaliacao-dashboard/page.tsx"
      provides: "Server component — renderiza v2 client"
  key_links:
    - from: "src/app/api/avaliacoes/dashboard/route.ts"
      to: "avaliacoes + funcionarios tables"
      via: "better-sqlite3 queries com JOIN e GROUP BY"
      pattern: "getDb\\(\\).prepare\\(.*FROM avaliacoes a JOIN funcionarios f"
    - from: "src/app/api/avaliacoes/dashboard/route.ts"
      to: "Score Final calculation (JS)"
      via: "map + Math.min/Math.max para score_base, penalidades, bonus"
      pattern: "Math\\.max\\(0, scoreBase - penalidades \\+ bonus\\)"
    - from: "src/app/(main)/dashboard/avaliacao-dashboard/_components/avaliacao-dashboard-v2-client.tsx"
      to: "/api/avaliacoes/dashboard"
      via: "fetch em useEffect com from/to params"
      pattern: "fetch\\(`/api/avaliacoes/dashboard"
    - from: "src/app/(main)/dashboard/avaliacao-dashboard/_components/avaliacao-dashboard-v2-client.tsx"
      to: "/api/avaliacoes/funcionario/[id]/historico"
      via: "fetch ao selecionar funcionário"
      pattern: "fetch\\(`/api/avaliacoes/funcionario/"
    - from: "src/app/(main)/dashboard/avaliacao-dashboard/_components/avaliacao-dashboard-v2-client.tsx"
      to: "fillCarryForward"
      via: "import + call após fetch do dashboard, antes de renderizar"
      pattern: "import \\{ fillCarryForward \\} from"
---

<objective>
**Fase:** Dashboard Avaliação v2 — Implementação completa

Redesign completo do dashboard de avaliação com layout de 2 colunas (65/35): KPIs da equipa à esquerda e detalhamento individual à direita. Score Final composto com penalidades/bónus, comparação semana a semana com carry forward, gráfico donut de distribuição, gráfico de linha com gradiente, barra de uso da ferramenta, tabela da equipa com estrelas e status, e painel individual com gráficos e comentários.

**Purpose:** Substituir o dashboard v1 (single column, médias simples) pelo v2 com Score Final composto, análise semanal e detalhamento individual — permitindo que gestores avaliem desempenho da equipa e de cada funcionário em profundidade.

**Output:** 3 novas API routes, 1 utility de carry forward, 12 novos componentes React, 1 página modificada.
</objective>

<execution_context>
@/home/jsilveira/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/jsilveira/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-dashboard-avaliacao-v2/05-CONTEXT.md
@.planning/phases/05-dashboard-avaliacao-v2/05-RESEARCH.md
@.planning/ROADMAP.md

<interfaces>
<!-- Types que o executor precisa. Extraídos do codebase existente. -->

Schema da tabela `avaliacoes` (src/lib/db.ts:56-83):
```sql
id INTEGER PRIMARY KEY AUTOINCREMENT
funcionario_id INTEGER NOT NULL
data_avaliacao TEXT NOT NULL
pontualidade INTEGER (1-10)
qualidade INTEGER (1-10)
produtividade INTEGER (1-10)
trabalho_equipa INTEGER (1-10)
iniciativa INTEGER (1-10)
comunicacao INTEGER (1-10)
media REAL (generated always as avg das 6 categorias, stored)
comentario TEXT
created_at DATETIME
atraso INTEGER DEFAULT 0
falta INTEGER DEFAULT 0
uso_ferramenta INTEGER DEFAULT 0
erro_critico INTEGER DEFAULT 0
perc_produtividade REAL
nota_auditoria REAL
tipo_auditoria TEXT ('supervisor' | 'auditor')
```

Schema da tabela `funcionarios` (src/lib/db.ts:40-53):
```sql
id INTEGER PRIMARY KEY AUTOINCREMENT
nome TEXT NOT NULL
ativo INTEGER NOT NULL DEFAULT 1
created_at DATETIME
updated_at DATETIME
```

Padrão de API route existente (stats/route.ts):
```typescript
import { type NextRequest, NextResponse } from "next/server";
import { getDb } from "@/lib/db";

export async function GET(request: NextRequest) {
  try {
    const { searchParams } = new URL(request.url);
    const db = getDb();
    // ... queries
    return NextResponse.json({ ... });
  } catch (error) {
    const message = error instanceof Error ? error.message : "Erro interno";
    return NextResponse.json({ error: message }, { status: 500 });
  }
}
```

Padrão de fetch existente (avaliacao-dashboard-client.tsx): `fetchStats` usa `useCallback` + `useEffect` + `useState`. Padrão de filtros reutilizável via `AvaliacaoFilters` component existente.

Padrão daily-chart.tsx para LineChart com gradient Area:
```tsx
<LineChart data={...}>
  <defs>
    <linearGradient id="fillNOME" x1="0" y1="0" x2="0" y2="1">
      <stop offset="5%" stopColor="var(--color-NOME)" stopOpacity={0.36} />
      <stop offset="95%" stopColor="var(--color-NOME)" stopOpacity={0.04} />
    </linearGradient>
  </defs>
  <CartesianGrid vertical={false} strokeOpacity={0.5} />
  <XAxis dataKey="week" tickLine={false} axisLine={false} tickMargin={8} fontSize={12} />
  <YAxis domain={[0, 100]} tickLine={false} axisLine={false} tickMargin={8} fontSize={12} />
  <ChartTooltip content={<ChartTooltipContent />} cursor={{ stroke: "var(--border)", strokeDasharray: "4 4" }} />
  <Area dataKey="valor" type="natural" fill="url(#fillNOME)" stroke="var(--color-NOME)" strokeWidth={1.25} dot={false} />
  <Line dataKey="valor" type="natural" stroke="var(--color-NOME)" strokeWidth={1.5} dot={false} />
</LineChart>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: API routes + carry forward + page scaffold</name>
  <files>
    src/app/api/avaliacoes/dashboard/route.ts
    src/app/api/avaliacoes/semanas/route.ts
    src/app/api/avaliacoes/funcionario/[id]/historico/route.ts
    src/lib/carry-forward.ts
    src/app/(main)/dashboard/avaliacao-dashboard/page.tsx
    src/app/(main)/dashboard/avaliacao-dashboard/_components/avaliacao-dashboard-v2-client.tsx
    src/app/(main)/dashboard/avaliacao-dashboard/_components/tendencia-indicator.tsx
  </files>
  <action>

    **Passo 1 — Criar `src/api/avaliacoes/dashboard/route.ts`**

    Criar a API route principal de dashboard v2. Seguir o código completo do RESEARCH.md (linhas 618-765) com as seguintes adaptações:

    1. Importar `getDb` de `@/lib/db` — mesmo padrão de `stats/route.ts`
    2. Query de comparativo semanal: `strftime('%Y-%W', a.data_avaliacao) AS week`, com 6 KPIs:
       - `produtividade`: `AVG(MIN(a.perc_produtividade, 100))` (cap em 100, per D5-01 e Pitfall 3)
       - `uso_speedops`: `AVG(CASE WHEN a.uso_ferramenta = 1 THEN 100.0 ELSE 0 END)`
       - `erros_criticos`: `SUM(a.erro_critico)`
       - `atrasos_faltas`: `SUM(a.atraso + a.falta)`
       - `media_auditor`: `AVG(CASE WHEN a.tipo_auditoria = 'auditor' THEN a.nota_auditoria END)`
       - `media_supervisor`: `AVG(CASE WHEN a.tipo_auditoria = 'supervisor' THEN a.nota_auditoria END)`
    3. Query de Score Final por funcionário (D5-01, D5-02):
       - SQL: `f.id, f.nome, AVG(MIN(a.perc_produtividade, 100)) AS avg_prod, AVG(a.nota_auditoria) AS avg_nota, AVG((a.pontualidade + a.qualidade + a.produtividade + a.trabalho_equipa + a.iniciativa + a.comunicacao) / 6.0) AS avg_categorias, SUM(a.atraso) AS total_atrasos, SUM(a.falta) AS total_faltas, SUM(a.erro_critico) AS total_erros, SUM(CASE WHEN a.uso_ferramenta = 1 THEN 1 ELSE 0 END) AS count_uso, AVG(CASE WHEN a.tipo_auditoria = 'auditor' THEN a.nota_auditoria END) AS media_auditor, AVG(CASE WHEN a.tipo_auditoria = 'supervisor' THEN a.nota_auditoria END) AS media_supervisor`
       - JS: calcular `scoreFinal`, `status`, e montar `distribuicao` array
    4. Query de uso ferramenta: duas queries (total count, uso_ferramenta=1 count)
    5. Retornar `{ comparativo_semanal, semana_atual, semana_anterior, team, distribuicao, uso_ferramenta }`
    6. Tratar `nota_auditoria` null como 0 (Pitfall 2): usar `?? 0` em todos os campos no JS
    7. Usar `CAST(... AS REAL)` para médias no SQL — necessário para evitar inteiros

    **Passo 2 — Criar `src/app/api/avaliacoes/semanas/route.ts`**

    Seguir código do RESEARCH.md (linhas 848-866):
    ```typescript
    // GET /api/avaliacoes/semanas
    // Retorna: { semanas: string[] } — semanas ISO com dados
    // Query: SELECT DISTINCT strftime('%Y-%W', data_avaliacao) AS week FROM avaliacoes ORDER BY week
    ```

    **Passo 3 — Criar `src/app/api/avaliacoes/funcionario/[id]/historico/route.ts`**

    Seguir código do RESEARCH.md (linhas 769-845):
    - Route handler com `params: Promise<{ id: string }>` (Next.js 16 padrão)
    - Query params: `from`, `to` (opcionais)
    - Query: `SELECT a.id, a.data_avaliacao, a.pontualidade, a.qualidade, a.produtividade, a.trabalho_equipa, a.iniciativa, a.comunicacao, a.media, a.comentario, a.atraso, a.falta, a.uso_ferramenta, a.erro_critico, a.perc_produtividade, a.nota_auditoria, a.tipo_auditoria FROM avaliacoes a WHERE a.funcionario_id = ? ORDER BY a.data_avaliacao ASC, a.id ASC`
    - Calcular `evolution` array: cada row → score final (mesma fórmula D5-01)
    - Filtrar comentários: rows com `comentario` não-nulo
    - Retornar `{ evolution, comentarios }`

    **Passo 4 — Criar `src/lib/carry-forward.ts`**

    Seguir código do RESEARCH.md (linhas 292-317):
    ```typescript
    export function fillCarryForward<T extends Record<string, unknown>>(
      weeks: string[],
      data: T[],
      kpis: string[],
    ): T[] {
      const dataMap = new Map(data.map((d) => [d.week as string, d]));
      const lastKnown: Record<string, unknown> = {};
      return weeks.map((week) => {
        if (dataMap.has(week)) {
          const entry = dataMap.get(week)!;
          for (const kpi of kpis) { if (entry[kpi] !== null && entry[kpi] !== undefined) lastKnown[kpi] = entry[kpi]; }
          return entry;
        }
        const carried = { ...({ week } as T) };
        for (const kpi of kpis) { (carried as Record<string, unknown>)[kpi] = lastKnown[kpi] ?? null; }
        return carried;
      });
    }
    ```

    **Passo 5 — Modificar `src/app/(main)/dashboard/avaliacao-dashboard/page.tsx`**

    Trocar import de client: importar `AvaliacaoDashboardV2Client` do novo arquivo em vez de `AvaliacaoDashboardClient`. Manter a estrutura do server component (header + client).

    ```tsx
    import { AvaliacaoDashboardV2Client } from "./_components/avaliacao-dashboard-v2-client";

    export default function AvaliacaoDashboardPage() {
      return (
        <div className="flex flex-col gap-6">
          <div className="flex items-center justify-between">
            <h1 className="font-semibold text-2xl">Dashboard — Avaliação e Desempenho</h1>
          </div>
          <AvaliacaoDashboardV2Client />
        </div>
      );
    }
    ```

    **Passo 6 — Criar `src/app/(main)/dashboard/avaliacao-dashboard/_components/tendencia-indicator.tsx`**

    Seguir código do RESEARCH.md (linhas 887-913):
    - Props: `current: number | null`, `previous: number | null`
    - Se ambos null: `—` (cinza)
    - Se diff < 0.01: `<Minus size={12} />` cinza com valor da diff
    - Se diff > 0: `<TrendingUp size={12} />` verde com `+diff`
    - Se diff < 0: `<TrendingDown size={12} />` vermelho com diff
    - Cores fixas por D5-17: verde `#22c55e`, vermelho `#ef4444`, cinza `#71717a`

    **Passo 7 — Criar scaffold de `src/app/(main)/dashboard/avaliacao-dashboard/_components/avaliacao-dashboard-v2-client.tsx`**

    Criar o client component principal (scaffold inicial, será expandido nas Tasks 2 e 3):
    1. `"use client"` — componente client obrigatório (Recharts, Select, fetch)
    2. Interface de resposta da API (DashboardResponse):
       - `comparativo_semanal: Array<{ week, produtividade, uso_speedops, erros_criticos, atrasos_faltas, media_auditor, media_supervisor }>`
       - `semana_atual / semana_anterior: same shape | null`
       - `team: Array<{ id, nome, avg_produtividade, total_atrasos, total_faltas, total_erros, uso_ferramenta, media_auditor, media_supervisor, score_final, status }>`
       - `distribuicao: Array<{ name, value, fill }>`
       - `uso_ferramenta: { utilizou_pct, utilizou_count, nao_utilizou_pct, nao_utilizou_count }`
    3. Interface HistoricoResponse:
       - `evolution: Array<{ data_avaliacao, score, perc_produtividade, tipo_auditoria }>`
       - `comentarios: Array<{ data, tipo, comentario }>`
    4. Estados: `data`, `loading`, `error`, `from`, `to`, `funcionarios`, `selectedFuncionarioId`, `selectedFuncionarioDetalhe`, `detalhe`, `detalheLoading`
    5. Fetch inicial: `useEffect` + `initializedRef` (mesmo padrão do v1 client)
       - Buscar `/api/avaliacoes/semanas` + `/api/funcionarios?ativos=true` em paralelo
       - Determinar from/to da última semana disponível ou usar range do último mês
       - Fetch `/api/avaliacoes/dashboard?from=&to=`
    6. `fetchDashboard`, `fetchDetalhe` com `useCallback`
    7. **Layout 2 colunas (65/35):** div grid no container principal:
       ```tsx
       <div className="grid grid-cols-1 gap-6 lg:grid-cols-[65%_35%]">
         <div className="flex flex-col gap-6">{/* LEFT COLUMN */}</div>
         <div className="flex flex-col gap-6">{/* RIGHT COLUMN */}</div>
       </div>
       ```
    8. Por enquanto, LEFT COLUMN com placeholder "Carregando..." e RIGHT COLUMN com mensagem "Selecione um funcionário para ver detalhes"
    9. **Filtros:** Reutilizar o componente `AvaliacaoFilters` existente, com:
       - Estado de funcionários populado do fetch inicial
       - `handlePeriodChange` → refetch dashboard
       - `handleFuncionarioChange` → set selectedFuncionarioId + fetch detalhe
    10. Função `handlePeriodChange` que aplica `fillCarryForward` nos dados do dashboard ANTES de setar no state. O carry forward gera a lista contígua de semanas entre `from` e `to` para evitar gaps no gráfico de linha (Pitfall 1)
    11. Render loading/error/empty states idêntico ao v1 client

  </action>
  <verify>
    <automated>
      # Verificar que as API routes existem e compilam
      ls src/app/api/avaliacoes/dashboard/route.ts
      ls src/app/api/avaliacoes/semanas/route.ts
      ls src/app/api/avaliacoes/funcionario/[id]/historico/route.ts
      ls src/lib/carry-forward.ts
      
      # Verificar compilação
      npx next build --no-lint 2>&1 | head -20 | grep -q "✓" && echo "BUILD OK" || echo "BUILD CHECK: see output"
      
      # Verificar que a página server component existe
      ls src/app/\(main\)/dashboard/avaliacao-dashboard/page.tsx
    </automated>
  </verify>
  <done>
    - API route `/api/avaliacoes/dashboard` retorna KPIs agregados + Score Final + distribuição + uso ferramenta
    - API route `/api/avaliacoes/semanas` retorna semanas disponíveis
    - API route `/api/avaliacoes/funcionario/:id/historico` retorna evolution + comentários
    - `fillCarryForward` exportada de `src/lib/carry-forward.ts`
    - `TendenciaIndicator` component criado com ícones lucide-react
    - Client component scaffold com fetch, estado, layout 2 colunas, filtros
    - Build compila sem erros
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Left column components — KPIs, charts, table</name>
  <files>
    src/app/(main)/dashboard/avaliacao-dashboard/_components/kpi-cards-v2.tsx
    src/app/(main)/dashboard/avaliacao-dashboard/_components/distribuicao-donut.tsx
    src/app/(main)/dashboard/avaliacao-dashboard/_components/ferramenta-bar.tsx
    src/app/(main)/dashboard/avaliacao-dashboard/_components/evolucao-prod-chart.tsx
    src/app/(main)/dashboard/avaliacao-dashboard/_components/star-rating.tsx
    src/app/(main)/dashboard/avaliacao-dashboard/_components/score-badge.tsx
    src/app/(main)/dashboard/avaliacao-dashboard/_components/equipa-table.tsx
    src/app/(main)/dashboard/avaliacao-dashboard/_components/avaliacao-dashboard-v2-client.tsx
  </files>
  <action>

    **Passo 1 — Criar `star-rating.tsx`**

    Componente puro (não precisa de "use client" pois só renderiza ícones, mas Star/StarHalf do lucide-react precisam). Usar `"use client"`.

    Seguir código do RESEARCH.md (linhas 464-495):
    - Props: `nota: number` (0-100), `size?: number` (default 14)
    - `starValue = nota / 20` (D5-22)
    - `full = Math.floor(starValue)`
    - `fraction = starValue - full`
    - `hasHalf = fraction >= 0.25 && fraction < 0.75`
    - `adjustedFull = fraction >= 0.75 ? full + 1 : full`
    - Renderizar 5 `<Star>`/`<StarHalf>` com cor `#eab308` (D5-17: amarelo para estrelas)

    **Passo 2 — Criar `score-badge.tsx`**

    Componente que recebe `status: string` retorna `<Badge>` colorido. Seguir código do RESEARCH.md (linhas 869-885):
    - Mapeamento de status → label + cores D5-17:
      - `excelente` → "Excelente", bg `#22c55e/20` text `#22c55e`
      - `bom` → "Bom", bg `#3b82f6/20` text `#3b82f6`
      - `atencao` → "Atenção", bg `#eab308/20` text `#eab308`
      - `critico` → "Crítico", variant `destructive`
    - Usar `shadcn/ui Badge` com className inline para cores

    **Passo 3 — Criar `kpi-cards-v2.tsx`**

    Componente "use client" que renderiza 6 cards KPI com dados do comparativo semanal (D5-07, D5-06):
    - Props: `semanaAtual: Semana | null`, `semanaAnterior: Semana | null`
    - 6 cards em grid `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-6` (adaptável)
    - Cada card:
      ```
      ┌──────────────────────┐
      │ Icon + Label         │ small text muted
      │ VALOR               │ large number
      │ ▲/▼/→ + diff       │ TendenciaIndicator
      └──────────────────────┘
      ```
    - Cards:
      1. **Produtividade Média** — `produtividade` da semana atual. Ícone: `TrendingUp` do lucide-react
      2. **Uso Speedops** — `uso_speedops` (%). Ícone: `CheckCircle2`
      3. **Erros Críticos** — `erros_criticos` (contagem). Ícone: `AlertTriangle`
      4. **Atrasos/Faltas** — `atrasos_faltas` (soma). Ícone: `Clock`
      5. **Média Auditor** — `media_auditor`. Ícone: `UserCheck`
      6. **Média Supervisor** — `media_supervisor`. Ícone: `UserCog`
    - Valor formatado: números com 1 casa decimal, percentuais com `%`
    - Usar `<Card>` do shadcn/ui (padrão do kpi-cards.tsx existente)
    - Importar `TendenciaIndicator` do arquivo criado na Task 1

    **Passo 4 — Criar `distribuicao-donut.tsx`**

    Componente "use client" com PieChart donut (D5-08). Seguir código RESEARCH.md (linhas 432-455):
    - Props: `data: Array<{ name: string; value: number; fill: string }>`, `total: number`
    - Usar `ChartContainer` com config das 4 categorias (como em Research.md linhas 604-610)
    - `<PieChart>` com `<Pie data={data} dataKey="value" nameKey="name" cx="50%" cy="50%" innerRadius={60} outerRadius={90} paddingAngle={2}>`
    - Label central: `<text x="50%" y="50%" textAnchor="middle" dominantBaseline="middle">` com `<tspan>` para total (bold, 2xl) e `<tspan>` para "Total" (small, muted-foreground)
    - Cores fixas D5-17: excelente `#22c55e`, bom `#3b82f6`, atencao `#eab308`, critico `#ef4444`
    - Tratar Pitfall 4: passar `config={chartConfig}` com entradas para cada categoria (não config vazio)

    **Passo 5 — Criar `ferramenta-bar.tsx`**

    Componente "use client" com barra horizontal (D5-09). Seguir código RESEARCH.md (linhas 508-539):
    - Props: `utilizouPct: number`, `naoUtilizouPct: number`, `utilizouCount: number`, `naoUtilizouCount: number`
    - Renderizar CSS (div) — mais simples que Recharts para 2 segmentos
    - Barra: `h-8 rounded-lg bg-muted overflow-hidden flex` com 2 divs filhas:
      - Azul `#3b82f6` — largura `utilizouPct`%
      - Vermelha `#ef4444` — largura `naoUtilizouPct`%
    - Legenda abaixo: `■ Utilizou: XX% (N)` + `■ Não utilizou: XX% (N)`
    - Mostrar texto % dentro da barra apenas se > 15% largura

    **Passo 6 — Criar `evolucao-prod-chart.tsx`**

    Componente "use client" com LineChart + gradient Area (D5-10). Seguir padrão do `daily-chart.tsx`:
    - Props: `data: Array<{ week: string; produtividade: number | null }>`
    - ChartConfig com label "Produtividade", color `var(--chart-1)`
    - `<defs><linearGradient id="fillProd">` + `<stop offset="5%" stopOpacity={0.36}>` + `<stop offset="95%" stopOpacity={0.04}>`
    - `<LineChart>` com dados, `<CartesianGrid vertical={false}>`, `<XAxis dataKey="week">`, `<YAxis domain={[0, 100]}>`, `<ChartTooltip>`
    - `<Area dataKey="produtividade" type="natural" fill="url(#fillProd)" stroke="var(--color-produtividade)">`
    - `<Line dataKey="produtividade" type="natural" stroke="var(--color-produtividade)">`
    - Envelopar em `<ChartContainer config={chartConfig}>` + `<Card>`
    - Título do card: "Evolução Média de Produtividade"

    **Passo 7 — Criar `equipa-table.tsx`**

    Componente "use client" com shadcn/ui Table (D5-11):
    - Props: `team: Array<TeamMember>` onde `TeamMember = { id, nome, total_atrasos, total_faltas, total_erros, uso_ferramenta, avg_produtividade, media_auditor, media_supervisor, score_final, status }`
    - Colunas:
      1. **Nome** — `f.nome`
      2. **Atrasos/Faltas** — `total_atrasos + total_faltas` com tooltip "X atraso(s) · Y falta(s)"
      3. **Speedops** — `uso_ferramenta ? "✓" : "✗"` com cor verde/vermelho (D5-17)
      4. **Erro Crítico** — `total_erros` com cor vermelha se > 0
      5. **Produtividade (%)** — `avg_produtividade?.toFixed(0)`% ou "---"
      6. **Avaliação Auditor** — `media_auditor?.toFixed(1)` + `<StarRating nota={media_auditor}>` 
      7. **Avaliação Supervisor** — `media_supervisor?.toFixed(1)` + `<StarRating nota={media_supervisor}>`
      8. **Score Final** — `score_final` em negrito
      9. **Status** — `<ScoreBadge status={status}>`
    - Usar `<Table>`, `<TableHeader>`, `<TableBody>`, `<TableRow>`, `<TableCell>` do shadcn/ui
    - Usar `<TableHead>` para cabeçalhos
    - Adicionar `onFuncionarioClick?: (id: number) => void` — ao clicar na linha, seleciona funcionário na coluna direita
    - Ordenar por `score_final` descendente (melhor score primeiro)

    **Passo 8 — Integrar no client component**

    Modificar `avaliacao-dashboard-v2-client.tsx` para renderizar a LEFT COLUMN completa:
    - Importar todos os novos componentes
    - LEFT COLUMN:
      ```tsx
      <div className="flex flex-col gap-6">
        <KpiCardsV2 semanaAtual={semana_atual} semanaAnterior={semana_anterior} />
        <Card><CardHeader><CardTitle>Distribuição de Desempenho</CardTitle></CardHeader><CardContent>
          <DistribuicaoDonut data={distribuicao} total={team.length} />
        </CardContent></Card>
        <Card><CardHeader><CardTitle>Uso da Ferramenta</CardTitle></CardHeader><CardContent>
          <FerramentaBar {...uso_ferramenta} />
        </CardContent></Card>
        <EvolucaoProdChart data={comparativo_semanal} />
        <Card><CardHeader><CardTitle>Equipa</CardTitle></CardHeader><CardContent>
          <EquipaTable team={team} onFuncionarioClick={handleFuncionarioClick} />
        </CardContent></Card>
      </div>
      ```
    - Implementar `handleFuncionarioClick(id)` que seta `selectedFuncionarioId` e faz fetch do histórico

  </action>
  <verify>
    <automated>
      # Verificar que todos os arquivos existem
      for f in \
        src/app/\(main\)/dashboard/avaliacao-dashboard/_components/kpi-cards-v2.tsx \
        src/app/\(main\)/dashboard/avaliacao-dashboard/_components/distribuicao-donut.tsx \
        src/app/\(main\)/dashboard/avaliacao-dashboard/_components/ferramenta-bar.tsx \
        src/app/\(main\)/dashboard/avaliacao-dashboard/_components/evolucao-prod-chart.tsx \
        src/app/\(main\)/dashboard/avaliacao-dashboard/_components/star-rating.tsx \
        src/app/\(main\)/dashboard/avaliacao-dashboard/_components/score-badge.tsx \
        src/app/\(main\)/dashboard/avaliacao-dashboard/_components/equipa-table.tsx; do
        ls "$f"
      done
      
      # Build check
      npx next build --no-lint 2>&1 | head -30 | grep -q "✓" && echo "BUILD OK" || echo "BUILD CHECK: see output"
    </automated>
  </verify>
  <done>
    - `KpiCardsV2` renderiza 6 cards com tendência ▲/▼/→
    - `DistribuicaoDonut` renderiza PieChart donut com 4 categorias + total central
    - `FerramentaBar` renderiza barra horizontal 2 segmentos com legenda
    - `EvolucaoProdChart` renderiza LineChart com área gradiente
    - `EquipaTable` renderiza tabela completa com StarRating, ScoreBadge, cores D5-17
    - LEFT COLUMN completa no client principal com todos os componentes
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Right column — individual detail components</name>
  <files>
    src/app/(main)/dashboard/avaliacao-dashboard/_components/funcionario-select.tsx
    src/app/(main)/dashboard/avaliacao-dashboard/_components/resumo-card.tsx
    src/app/(main)/dashboard/avaliacao-dashboard/_components/score-evol-chart.tsx
    src/app/(main)/dashboard/avaliacao-dashboard/_components/prod-hist-chart.tsx
    src/app/(main)/dashboard/avaliacao-dashboard/_components/comentarios-list.tsx
    src/app/(main)/dashboard/avaliacao-dashboard/_components/avaliacao-dashboard-v2-client.tsx
  </files>
  <action>

    **Passo 1 — Criar `funcionario-select.tsx`**

    Componente "use client" com shadcn/ui Select (D5-12):
    - Props: `funcionarios: Array<{ id: number; nome: string }>`, `selectedId: number | null`, `onChange: (id: number) => void`
    - Label "Funcionário:" + `<Select value={String(selectedId ?? "")} onValueChange={(v) => onChange(Number(v))}>`
    - Filtrar apenas funcionários que estão no `team` (têm dados no período) e mostrar como opções
    - Opção "Selecionar funcionário" desabilitada como placeholder
    - Botão "Limpar" ao lado para desselecionar
    - Usar padrão de `avaliacao-filters.tsx` existente

    **Passo 2 — Criar `resumo-card.tsx`**

    Componente "use client" (D5-13):
    - Props: `funcionario: { nome: string, score_final: number, status: string, avg_produtividade: number|null, media_auditor: number|null, media_supervisor: number|null, total_atrasos: number, total_faltas: number, total_erros: number }`
    - Layout:
      ```
      ┌─────────────────────────┐
      │ Nome do Funcionário     │ heading
      │ ┌─────────────────────┐ │
      │ │   SCORE FINAL       │ │ grande (text-5xl) + medalha (Award icon)
      │ │   Status (badge)    │ │
      │ └─────────────────────┘ │
      │ Prod □□□   Atrasos □□  │ grid 2-col métricas
      │ Auditor □□  Erros □□   │
      └─────────────────────────┘
      ```
    - Medalha: `<Award className="size-6" />` do lucide-react com cor baseada no status (verde para excelente, azul para bom, etc.)
    - Grid 2 colunas com métricas secundárias: Produtividade, Média Auditor, Média Supervisor, Atrasos+Faltas, Erros
    - Usar `<Card>` do shadcn/ui

    **Passo 3 — Criar `score-evol-chart.tsx`**

    Componente "use client" com LineChart + gradient (D5-14):
    - Props: `data: Array<{ data_avaliacao: string; score: number }>`
    - Mesmo padrão de `evolucao-prod-chart.tsx` (gradient Area + Line)
    - ChartConfig com label "Score", color `var(--chart-2)`
    - XAxis dataKey: `data_avaliacao` com tickFormatter para formato BR (dd/mm)
    - YAxis domain: `[0, 100]`
    - Title: "Evolução do Score"
    - Wrapped in `<Card>` + `<ChartContainer>`

    **Passo 4 — Criar `prod-hist-chart.tsx`**

    Componente "use client" com BarChart (D5-15):
    - Props: `data: Array<{ data_avaliacao: string; perc_produtividade: number | null }>`
    - Usar `<BarChart>` do Recharts com `<Bar dataKey="perc_produtividade" radius={[4, 4, 0, 0]} fill="var(--color-produtividade)">`
    - ChartConfig: label "Produtividade", color `var(--chart-1)`
    - XAxis: `data_avaliacao`, YAxis: `[0, 100]`
    - Tooltip com `<ChartTooltipContent>`
    - Title: "Histórico de Produtividade"
    - Wrapped in `<Card>` + `<ChartContainer>`

    **Passo 5 — Criar `comentarios-list.tsx`**

    Componente "use client" (D5-16):
    - Props: `comentarios: Array<{ data: string; tipo: string; comentario: string }>`
    - Se vazio: "Nenhum comentário registado."
    - Lista de cards (ou divs) com:
      - Data formatada (dd/mm/aaaa)
      - Tipo: badge "Supervisor" ou "Auditor" com cor azul/verde
      - Comentário em texto
    - Ordenar do mais recente para o mais antigo
    - Wrapped in `<Card>` com título "Comentários"

    **Passo 6 — Integrar RIGHT COLUMN no client component**

    Modificar `avaliacao-dashboard-v2-client.tsx`:
    - Importar todos os novos componentes: `FuncionarioSelect`, `ResumoCard`, `ScoreEvolChart`, `ProdHistChart`, `ComentariosList`
    - RIGHT COLUMN:
      ```tsx
      <div className="flex flex-col gap-6">
        <FuncionarioSelect
          funcionarios={team.map(t => ({ id: t.id, nome: t.nome }))}
          selectedId={selectedFuncionarioId}
          onChange={handleFuncionarioSelect}
        />
        {selectedFuncionarioId && detalhe ? (
          <>
            <ResumoCard funcionario={team.find(t => t.id === selectedFuncionarioId)!} />
            <ScoreEvolChart data={detalhe.evolution} />
            <ProdHistChart data={detalhe.evolution} />
            <ComentariosList comentarios={detalhe.comentarios} />
          </>
        ) : (
          <div className="flex items-center justify-center rounded-lg border bg-card p-12">
            <p className="text-muted-foreground">Selecione um funcionário para ver detalhes</p>
          </div>
        )}
      </div>
      ```
    - `handleFuncionarioSelect(id)`: seta `selectedFuncionarioId`, faz fetch `/api/avaliacoes/funcionario/${id}/historico`
    - `handleFuncionarioClick(id)` (da EquipaTable click) → chama `handleFuncionarioSelect(id)` — mesma lógica
    - Estado `detalheLoading` com spinner enquanto carrega detalhe
    - Tratar erro no fetch do detalhe com toast ou mensagem inline

    **Passo 7 — Final polishing**
    
    - Garantir que o layout 65/35 funciona em mobile (1 coluna, empilhado)
    - Garantir que `AvaliacaoFilters` existente é reutilizado com os mesmos props
    - Garantir que todas as cores seguem D5-17:
      - `#22c55e` (verde): Excelente, positivo, Sim, utilizou
      - `#ef4444` (vermelho): Crítico, negativo, Não, não utilizou, erros
      - `#3b82f6` (azul): Bom, info
      - `#eab308` (amarelo): Atenção, estrelas
      - `#71717a` (cinza): estável, neutro

  </action>
  <verify>
    <automated>
      # Verificar que todos os arquivos existem
      for f in \
        src/app/\(main\)/dashboard/avaliacao-dashboard/_components/funcionario-select.tsx \
        src/app/\(main\)/dashboard/avaliacao-dashboard/_components/resumo-card.tsx \
        src/app/\(main\)/dashboard/avaliacao-dashboard/_components/score-evol-chart.tsx \
        src/app/\(main\)/dashboard/avaliacao-dashboard/_components/prod-hist-chart.tsx \
        src/app/\(main\)/dashboard/avaliacao-dashboard/_components/comentarios-list.tsx; do
        ls "$f"
      done
      
      # Build check — toda a fase compila junto
      npx next build --no-lint 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    - `FuncionarioSelect` renderiza dropdown com funcionários do team
    - `ResumoCard` exibe Score Final grande com medalha + métricas em grid
    - `ScoreEvolChart` renderiza LineChart com área gradiente para score individual
    - `ProdHistChart` renderiza BarChart para produtividade histórica
    - `ComentariosList` renderiza lista de comentários ordenada
    - RIGHT COLUMN completa com funcionamento integrado ao clique na tabela
    - Layout responsivo 65/35 com fallback mobile
    - Build compila sem erros
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | HTTP request with query params (from, to, funcionario_id) crosses from browser to Next.js server |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | API routes | accept | Aplicação single-user local — sem autenticação multi-usuário. Risco aceito. |
| T-05-02 | Tampering | Query params (from/to) | mitigate | API routes validam que `from` e `to` estão presentes (retornam 400 se ausentes). Sem mutations, params apenas filtram leitura. |
| T-05-03 | Repudiation | N/A | accept | Fase sem mutations — apenas leitura de dados. Sem operações para repudiar. |
| T-05-04 | Information Disclosure | API routes | accept | Single-user local — sem dados de outros usuários. Risco aceito. |
| T-05-05 | Denial of Service | API routes | accept | Single-user local sem limite de queries no MVP. Risco aceito para ambiente local. |
| T-05-06 | Elevation of Privilege | N/A | accept | Sem autenticação ou níveis de acesso nesta fase. |
</threat_model>

<verification>
### Verificação geral da fase

Após completar todos os 3 tasks:

1. **Build check:** `npx next build --no-lint` compila sem erros
2. **API check:** 
   ```bash
   curl -s "http://localhost:3000/api/avaliacoes/semanas" | jq '.semanas | length'
   curl -s "http://localhost:3000/api/avaliacoes/dashboard?from=2026-01-01&to=2026-12-31" | jq '.team | length'
   curl -s "http://localhost:3000/api/avaliacoes/funcionario/1/historico?from=2026-01-01&to=2026-12-31" | jq '.evolution | length'
   ```
3. **Lint check:** `npx biome check src/app/api/avaliacoes/dashboard/route.ts src/app/api/avaliacoes/semanas/route.ts src/app/api/avaliacoes/funcionario/\[id\]/historico/route.ts src/lib/carry-forward.ts`
4. **Score Final verification:** O score_final do funcionário com maior prod = `Math.max(0, scoreBase - penalidades + bonus)` com scoreBase = `(prod capped 100 + nota_auditoria + media_categorias*10) / 3`
</verification>

<success_criteria>

### Critérios de Sucesso (Goal-Backward)

1. **Score Final calculado corretamente:** D5-01 fórmula implementada em JS nas 2 API routes — `Math.min(perc_produtividade, 100)` capped, penalidades subtraídas, bónus adicionado, `Math.max(0, resultado)`. Null tratados como 0.

2. **6 cards KPI com tendência:** D5-07 cards renderizados com D5-06 TendenciaIndicator. Cada card mostra valor atual + indicador ▲/▼/→ comparando com semana anterior.

3. **Donut de distribuição:** D5-08 PieChart com innerRadius+outerRadius, label central com total. Cores D5-17.

4. **Barra de uso da ferramenta:** D5-09 barra horizontal 2 segmentos (azul/vermelho) com legenda.

5. **Gráfico linha com gradiente:** D5-10 LineChart + gradient Area, mesmo padrão de daily-chart.tsx.

6. **Tabela da equipa:** D5-11 tabela com 9 colunas, StarRating (D5-22), ScoreBadge (D5-02), cores D5-17.

7. **Detalhamento individual:** D5-12 a D5-16 — dropdown, resumo com medalha, gráfico evolução score (gradiente), gráfico produtividade (barras), lista de comentários.

8. **Comparativo semanal:** D5-03 ISO week agrupamento, D5-04 última vs anterior, D5-05 carry forward client-side.

9. **API dedicada:** D5-18, D5-19, D5-20 — 3 novas API routes sem modificar as existentes.

10. **Sem novas tabelas:** D5-21 — todas as queries usam tabelas existentes `avaliacoes` e `funcionarios`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dashboard-avaliacao-v2/05-01-SUMMARY.md`
</output>

---
phase: 03-kpi-dashboard
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/app/(main)/dashboard/kpi/page.tsx
  - src/app/(main)/dashboard/kpi/_components/kpi-dashboard-client.tsx
  - src/app/(main)/dashboard/kpi/_components/period-filter.tsx
  - src/app/(main)/dashboard/kpi/_components/kpi-cards.tsx
  - src/app/(main)/dashboard/kpi/_components/daily-chart.tsx
  - src/app/(main)/dashboard/kpi/_components/channel-chart.tsx
  - src/app/(main)/dashboard/kpi/_components/cil-chart.tsx
  - src/app/(main)/dashboard/kpi/_components/cil-modal.tsx
  - src/app/(main)/dashboard/kpi/_components/carteira-modal.tsx
  - src/app/(main)/dashboard/kpi/_components/tipology-section.tsx
  - src/app/(main)/dashboard/upload/_components/upload-area.tsx
autonomous: true
requirements: [KPI-01, KPI-02, KPI-03]

must_haves:
  truths:
    - "Página /dashboard/kpi carrega e exibe filtro de período no topo"
    - "Dropdown de mês lista meses disponíveis do banco"
    - "Selecionar mês → date range reflete o mês inteiro"
    - "Cards KPI exibem: Total de Pedidos, Carteira, Crescimento, Média, Dia Máximo"
    - "Valores formatados com Intl.NumberFormat('pt-PT')"
    - "Carteira é editável: clique abre modal com input numérico"
    - "Gráfico de evolução diária (LineChart) renderiza com dados do período"
    - "Gráfico de distribuição por canal (PieChart donut) renderiza"
    - "Gráfico de pedidos por canal (BarChart horizontal) renderiza"
    - "Gráfico de CILs (BarChart) com barras clicáveis renderiza"
    - "Clicar barra CIL abre modal com tabela de registros"
    - "Modal CIL tem filtro por Estado e botão exportar CSV"
    - "Seção de tipologias exibe top 5 justificação por canal"
    - "Após upload bem-sucedido, toast tem ação 'Ver Dashboard'"
    - "Dados são re-fetch ao mudar período"
  artifacts:
    - path: "src/app/(main)/dashboard/kpi/page.tsx"
      provides: "Server Component page shell"
      min_lines: 10
    - path: "src/app/(main)/dashboard/kpi/_components/kpi-dashboard-client.tsx"
      provides: "Client orchestrator — fetches data, manages period state"
      min_lines: 60
    - path: "src/app/(main)/dashboard/kpi/_components/period-filter.tsx"
      provides: "Month dropdown + date range picker, synchronized"
      min_lines: 50
    - path: "src/app/(main)/dashboard/kpi/_components/kpi-cards.tsx"
      provides: "5 KPI cards in responsive grid"
      min_lines: 60
    - path: "src/app/(main)/dashboard/kpi/_components/daily-chart.tsx"
      provides: "LineChart for daily evolution"
      min_lines: 40
    - path: "src/app/(main)/dashboard/kpi/_components/channel-chart.tsx"
      provides: "PieChart donut + BarChart horizontal for channels"
      min_lines: 60
    - path: "src/app/(main)/dashboard/kpi/_components/cil-chart.tsx"
      provides: "BarChart clicável for CILs"
      min_lines: 40
    - path: "src/app/(main)/dashboard/kpi/_components/cil-modal.tsx"
      provides: "Dialog + @tanstack/react-table + Estado filter + CSV export"
      min_lines: 80
    - path: "src/app/(main)/dashboard/kpi/_components/carteira-modal.tsx"
      provides: "Dialog + numeric input + Save/Cancel"
      min_lines: 40
    - path: "src/app/(main)/dashboard/kpi/_components/tipology-section.tsx"
      provides: "Top 5 tipologias per channel cards"
      min_lines: 30
  key_links:
    - from: "kpi-dashboard-client.tsx"
      to: "GET /api/data?from=&to="
      via: "fetch on mount and on period change"
      pattern: "fetch\\(.*api/data"
    - from: "kpi-dashboard-client.tsx"
      to: "GET /api/cil?from=&to="
      via: "fetch on period change"
      pattern: "fetch\\(.*api/cil"
    - from: "kpi-dashboard-client.tsx"
      to: "GET /api/carteira?mes="
      via: "fetch on period change"
      pattern: "fetch\\(.*api/carteira"
    - from: "upload-area.tsx"
      to: "/dashboard/kpi"
      via: "toast action navigation on upload success"
      pattern: "router.push.*kpi"
---

<objective>
Página completa `/dashboard/kpi` com dashboard de KPIs: filtro de período, cards de métricas, 4 gráficos Recharts, seção de canais/tipologias, modal CIL com tabela e export CSV, modal de carteira editável, e navegação pós-upload.

Purpose: Entregar a visualização principal do projeto — usuários veem KPIs, gráficos e detalhamentos dos dados importados.
Output: Página KPI funcional com todos os componentes, consumindo as APIs do Plano 01.
</objective>

<execution_context>
@/home/jsilveira/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/jsilveira/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-kpi-dashboard/03-CONTEXT.md
@.planning/phases/03-kpi-dashboard/03-RESEARCH.md
@.planning/phases/03-kpi-dashboard/03-01-SUMMARY.md

<interfaces>
<!-- Type contract for the data API response (consumed by all frontend components) -->

GET /api/data?from=...&to=... response:
```typescript
interface KpiDataResponse {
  total: number;
  canais: Array<{ canal: string; count: number }>;
  daily: Array<{ date: string; count: number }>;
  meses_disponiveis: Array<{ mes_competencia: string }>;
  crescimento: { value: number | null; label: string | null };
  workdays: number;
  avg_daily: number;
  max_day: { date: string; count: number } | null;
  tipologias_por_canal: Record<string, Array<{ justificacao: string; count: number }>>;
}
```

GET /api/cil?from=...&to=... response:
```typescript
interface CilResponse {
  cil_aggregates: Array<{ cil: string; count: number }>;
}
```

GET /api/cil/[cil]?from=...&to=...&estado=... response:
```typescript
interface CilDetailResponse {
  total: number;
  records: Array<Record<string, string>>;
}
```

GET /api/carteira?mes=YYYY-MM response:
```typescript
interface CarteiraResponse {
  total_clientes: number | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server page, dashboard client orchestrator, period filter, upload navigation</name>

  <read_first>
  - src/app/(main)/dashboard/upload/_components/upload-area.tsx (toast pattern existing)
  - src/app/(main)/dashboard/default/page.tsx (server page pattern)
  - src/components/ui/select.tsx (shadcn Select for month dropdown)
  - src/components/ui/button.tsx (for navigation button in toast)
  - src/components/date-range-picker.tsx (existing date range picker)
  </read_first>

  <files>
  src/app/(main)/dashboard/kpi/page.tsx,
  src/app/(main)/dashboard/kpi/_components/kpi-dashboard-client.tsx,
  src/app/(main)/dashboard/kpi/_components/period-filter.tsx,
  src/app/(main)/dashboard/upload/_components/upload-area.tsx
  </files>

  <action>
  1.1 — Criar diretório: src/app/(main)/dashboard/kpi/_components/

  1.2 — src/app/(main)/dashboard/kpi/page.tsx (Server Component — per D-04):
    ```tsx
    import { KpiDashboardClient } from "./_components/kpi-dashboard-client";

    export default function KpiDashboardPage() {
      return (
        <div className="flex flex-col gap-6">
          <div className="flex items-center justify-between">
            <h1 className="font-semibold text-2xl">Dashboard de KPIs</h1>
          </div>
          <KpiDashboardClient />
        </div>
      );
    }
    ```

  1.3 — src/app/(main)/dashboard/kpi/_components/kpi-dashboard-client.tsx (Client Component — D-41):
    "use client" — orchestrator que gerencia estado de período e dados.
    - Estados: { data: KpiDataResponse | null, cilData: CilResponse | null, carteira: number | null, loading: boolean, error: string | null }
    - useEffect no mount e quando período muda: fetch GET /api/data?from=X&to=Y, GET /api/cil?from=X&to=Y, GET /api/carteira?mes=Z
    - Estado de período: { from: string, to: string, selectedMonth: string | null }
    - Padrão (D-11): último mês de meses_disponiveis (ou range completo se vazio)
    - Render: PeriodFilter → KpiCards → grid 2-col [DailyChart + ChannelCharts] → CilChart → TipologySection → CilModal → CarteiraModal
    - Callbacks: onPeriodChange(newFrom, newTo, newMonth), onCilClick(cil), onCarteiraEdit()

  1.4 — src/app/(main)/dashboard/kpi/_components/period-filter.tsx (Client Component — D-06, D-08, D-09, D-10, D-11):
    "use client" — props: { meses: string[], from: string, to: string, onPeriodChange }
    - Dropdown de mês: Select do shadcn com opções dos meses disponíveis + "Personalizado"
    - Se mês selecionado → calcular from/to como primeiro e último dia do mês
    - Date range: Input de datas (react-day-picker via componente existente ou Input type="date" para simplificar)
    - Sincronização (D-09): dropdown → range reflete mês; range livre → dropdown "Personalizado"
    - Layout: lado a lado (flex gap-4 items-center)

  1.5 — src/app/(main)/dashboard/upload/_components/upload-area.tsx (MODIFICAR — D-39):
    Após upload bem-sucedido (no .then do fetch), adicionar ao toast.success:
    ```typescript
    import { useRouter } from "next/navigation"; // já deve existir no componente ou adicionar
    const router = useRouter();

    // No .then do fetch, após toast.success:
    toast.success(`Upload realizado: ${data.rows_imported} linhas importadas, ${data.rows_ignored} ignoradas`, {
      action: {
        label: "Ver Dashboard",
        onClick: () => router.push("/dashboard/kpi"),
      },
    });
    ```
    OBS: O componente é "use client" — pode usar useRouter().
  </action>

  <acceptance_criteria>
  1. Página /dashboard/kpi carrega sem erro 500
  2. Toast de upload bem-sucedido exibe botão "Ver Dashboard"
  3. Clicar "Ver Dashboard" navega para /dashboard/kpi
  4. Dashboard client renderiza PeriodFilter com meses do banco
  5. Selecionar mês no dropdown atualiza o range
  6. Loading state visível durante fetch inicial
  </acceptance_criteria>

  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/dashboard/kpi</automated>
  </verify>

  <done>Estrutura base do dashboard funcionando: página carrega, fetch de dados, filtro de período, navegação pós-upload</done>
</task>

<task type="auto">
  <name>Task 2: KPI cards + daily chart + channel charts + tipology section</name>

  <read_first>
  - .planning/phases/03-kpi-dashboard/03-CONTEXT.md (D-12, D-13, D-14, D-15, D-16, D-17, D-19, D-22)
  - .planning/phases/03-kpi-dashboard/03-RESEARCH.md (Pattern 2, Pattern 3, Business Days)
  - src/app/(main)/dashboard/default/_components/metric-cards.tsx (pattern de Card + Badge)
  - src/app/(main)/dashboard/ecommerce/_components/kpi-strip.tsx (pattern de ChartContainer)
  - src/components/ui/card.tsx (Card, CardHeader, CardTitle, CardDescription, CardContent)
  - src/components/ui/chart.tsx (ChartConfig, ChartContainer, ChartTooltip, ChartTooltipContent)
  </read_first>

  <files>
  src/app/(main)/dashboard/kpi/_components/kpi-cards.tsx,
  src/app/(main)/dashboard/kpi/_components/daily-chart.tsx,
  src/app/(main)/dashboard/kpi/_components/channel-chart.tsx,
  src/app/(main)/dashboard/kpi/_components/tipology-section.tsx
  </files>

  <action>
  2.1 — src/app/(main)/dashboard/kpi/_components/kpi-cards.tsx (D-12, D-13, D-14):
    "use client" — props: { total, carteira, crescimento, avg_daily, max_day, workdays, onCarteiraClick }
    Grid responsivo: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5 gap-4`
    5 cards (cada um com Card → CardHeader → CardTitle + CardDescription + CardContent):
    - **Total de Pedidos**: Ícone BarChart3, valor formatado com Intl.NumberFormat("pt-PT"), badge crescimento
    - **Carteira de Clientes**: Ícone Users, valor clicável (onClick → onCarteiraClick), subtítulo "Clique para editar"
    - **Crescimento Líquido**: Ícone TrendingUp, badge com percentual (verde se >0, vermelho se <0), subtítulo crescimento.label
    - **Média Diária (dias úteis)**: Ícone Calendar, avg_daily formatado, subtítulo "{workdays} dias úteis"
    - **Dia com maior volume**: Ícone ArrowUpRight, {max_day.count} na data {max_day.date formatada}
    CRÍTICO: Formatar números com `new Intl.NumberFormat("pt-PT").format(valor)` — D-14.
    Crescimento usa Badge do shadcn: variant="default" se >= 0, "destructive" se < 0. Ícone TrendingUp/TrendingDown.

  2.2 — src/app/(main)/dashboard/kpi/_components/daily-chart.tsx (D-15, D-19):
    "use client" — props: { data: Array<{ date: string, count: number }> }
    ```tsx
    import { ChartContainer, ChartTooltip, ChartTooltipContent, type ChartConfig } from "@/components/ui/chart";
    import { LineChart, Line, XAxis, YAxis, CartesianGrid } from "recharts";

    const chartConfig = {
      pedidos: { label: "Pedidos", color: "var(--chart-1)" },
    } satisfies ChartConfig;

    <div className="rounded-xl border bg-card p-4">
      <h3 className="mb-4 font-semibold text-sm">Evolução Diária</h3>
      <ChartContainer config={chartConfig} className="h-72 w-full">
        <LineChart data={data} margin={{ top: 5, right: 10, left: 10, bottom: 0 }}>
          <CartesianGrid vertical={false} />
          <XAxis dataKey="date" tickLine={false} axisLine={false} tickMargin={8} fontSize={12} />
          <YAxis tickLine={false} axisLine={false} tickMargin={8} fontSize={12} />
          <ChartTooltip content={<ChartTooltipContent />} cursor={{ stroke: "var(--border)", strokeDasharray: "4 4" }} />
          <Line dataKey="count" stroke="var(--color-pedidos)" strokeWidth={2} dot={false} activeDot={{ r: 4 }} />
        </LineChart>
      </ChartContainer>
    </div>
    ```

  2.3 — src/app/(main)/dashboard/kpi/_components/channel-chart.tsx (D-16, D-17, D-19):
    "use client" — props: { data: Array<{ canal: string, count: number }> }
    Dois gráficos lado a lado em grid: `grid grid-cols-1 lg:grid-cols-2 gap-4`
    - **Distribuição por Canal** (PieChart donut):
      ChartContainer + PieChart + Pie com innerRadius={60} outerRadius={80}
      Cada slice: Cell com fill `var(--chart-${index + 1})`
      ChartTooltipContent mostrando canal + percentual
    - **Pedidos por Canal** (BarChart horizontal):
      ChartContainer + BarChart layout="vertical" com XAxis type="number" + YAxis type="category" dataKey="canal"
      Barras com radius={[0,4,4,0]}, Cell com fill `var(--chart-${index + 1})`
    ChartConfig dinâmica: gerar a partir dos canais recebidos

  2.4 — src/app/(main)/dashboard/kpi/_components/tipology-section.tsx (D-22):
    "use client" — props: { tipologiasPorCanal: Record<string, Array<{ justificacao: string, count: number }>> }
    Grid de cards: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4`
    Para cada canal (Back-Office, Email, Portal, Telefone Front):
    - Card com CardHeader + CardTitle (nome do canal)
    - CardContent com lista ordenada (1. {justificacao} — {count})
    - Se canal não tiver tipologias no período, mostrar "Sem dados"
  </action>

  <acceptance_criteria>
  1. 5 KPI cards renderizam em grid responsivo com valores do período
  2. Carteira card é clicável (abre modal — integração com Task 3)
  3. Crescimento mostra badge verde/vermelho com percentual
  4. Daily chart renderiza LineChart com tooltip
  5. Channel charts renderizam donut + barra horizontal lado a lado
  6. Tipology section mostra top 5 por canal em cards
  7. Todos os valores formatados com pt-PT
  </acceptance_criteria>

  <verify>
    <automated>MISSING — verification is visual. Run dev server and navigate to /dashboard/kpi</automated>
  </verify>

  <done>KPIs e gráficos renderizam com dados reais do período selecionado</done>
</task>

<task type="auto">
  <name>Task 3: CIL chart + CIL modal + Carteira modal</name>

  <read_first>
  - .planning/phases/03-kpi-dashboard/03-CONTEXT.md (D-18, D-23 a D-27, D-29 a D-31)
  - .planning/phases/03-kpi-dashboard/03-RESEARCH.md (Pattern 3 Clicável Bar, CilModal, CarteiraModal)
  - src/components/ui/dialog.tsx (Dialog, DialogContent, DialogHeader, DialogTitle)
  - src/components/ui/select.tsx (Select for Estado filter)
  - src/components/ui/table.tsx (Table, TableHeader, TableBody, TableRow, TableCell)
  - src/components/ui/button.tsx (Button for export CSV)
  - src/app/(main)/dashboard/kpi/_components/kpi-dashboard-client.tsx (props de callback)
  </read_first>

  <files>
  src/app/(main)/dashboard/kpi/_components/cil-chart.tsx,
  src/app/(main)/dashboard/kpi/_components/cil-modal.tsx,
  src/app/(main)/dashboard/kpi/_components/carteira-modal.tsx
  </files>

  <action>
  3.1 — src/app/(main)/dashboard/kpi/_components/cil-chart.tsx (D-18):
    "use client" — props: { data: Array<{ cil: string, count: number }>, onCilClick: (cil: string) => void }
    - BarChart layout="vertical" em ChartContainer
    - XAxis type="number", YAxis type="category" dataKey="cil"
    - Bar com fill="var(--chart-1)", radius={[0,4,4,0]}
    - CRÍTICO: onClick no elemento Bar:
      ```tsx
      <Bar
        dataKey="count"
        cursor="pointer"
        onClick={(entry) => {
          if (entry?.cil) onCilClick(entry.cil);
        }}
      />
      ```
    - Título: "Chamados por CIL (Top 20)"
    - Altura: h-80
    - Se data vazio: mostrar "Nenhum dado de CIL no período"

  3.2 — src/app/(main)/dashboard/kpi/_components/cil-modal.tsx (D-23 a D-27):
    "use client" — props: { cil: string | null, from: string, to: string, onClose: () => void }
    - Dialog do shadcn: open={cil !== null}, onOpenChange → onClose se fechar
    - Ao abrir (useEffect quando cil muda), fetch GET /api/cil/{cil}?from={from}&to={to}
    - Estado interno: { records, total, loading, estadoFilter }
    - **Header** (D-27): "CIL {cil} — {total} registros"
    - **Filtro por Estado** (D-25): Select com opções "Todos", "Fechado", "Pendente", "Aberto"
      - Ao mudar, re-fetch com ?estado= adicionado
    - **Tabela** (D-24): Usar @tanstack/react-table simples (sem paginação complexa):
      - Colunas dinâmicas a partir das chaves do primeiro record
      - Ou colunas fixas: id_contacto, estado, dt_registo, dt_contato, justificação, receção, cil
      - Body com scroll: max-h-96 overflow-y-auto
    - **Contagem** (D-27): "Exibindo {records.length} de {total} registros"
    - **Exportar CSV** (D-26): Botão que gera CSV client-side:
      ```typescript
      function exportCsv() {
        if (records.length === 0) return;
        const headers = Object.keys(records[0]);
        const csvContent = [
          headers.join(","),
          ...records.map(r => headers.map(h => `"${String(r[h] ?? "").replace(/"/g, '""')}"`).join(","))
        ].join("\n");
        const blob = new Blob([csvContent], { type: "text/csv;charset=utf-8;" });
        const url = URL.createObjectURL(blob);
        const a = document.createElement("a");
        a.href = url;
        a.download = `cil-${cil}-${from}-${to}.csv`;
        a.click();
        URL.revokeObjectURL(url);
      }
      ```
    - Tamanho do Dialog: max-w-4xl

  3.3 — src/app/(main)/dashboard/kpi/_components/carteira-modal.tsx (D-29, D-30, D-31):
    "use client" — props: { open: boolean, mes: string | null, currentValue: number | null, onSave: (value: number) => void, onClose: () => void }
    - Dialog pequeno do shadcn
    - Título: "Carteira de Clientes — {mes}"
    - Input numérico (type="number", min=0):
      ```tsx
      <Input
        type="number"
        min={0}
        step={1}
        value={valor}
        onChange={(e) => setValor(Number(e.target.value))}
      />
      ```
    - Botões: Cancelar + Guardar (shadcn Button variant="outline" e "default")
    - Guardar → POST /api/carteira com { mes_competencia: mes, total_clientes: valor } → onSave(valor) → onClose()
    - Se período multi-mês (D-30): usar mês final do range (ex: 2026-01 a 2026-03 → mes="2026-03")

  3.4 — WIRING no kpi-dashboard-client.tsx (modificar se criado na Task 1):
    Adicionar estados: selectedCil, carteiraModalOpen, carteiraMes
    Adicionar callbacks:
    - handleCilClick(cil) → setSelectedCil(cil)
    - handleCarteiraClick() → setCarteiraModalOpen(true), calcular mes do período atual
    - handleCarteiraSave(value) → atualizar estado local + re-fetch carteira
    Render condicional: {selectedCil && <CilModal ... />}
    Render condicional: {carteiraModalOpen && <CarteiraModal ... />}
  </action>

  <acceptance_criteria>
  1. CIL chart exibe top 20 CILs com barras clicáveis
  2. Clicar barra CIL abre modal com tabela de registros
  3. Modal CIL exibe contagem total e filtrada
  4. Filtro Estado no modal re-fetch os dados
  5. Botão Exportar CSV baixa arquivo com dados filtrados
  6. Carteira modal abre com valor atual pré-preenchido
  7. Salvar carteira faz POST /api/carteira e atualiza card
  8. Fechar modal com Cancelar não persiste alterações
  </acceptance_criteria>

  <verify>
    <automated>MISSING — verification is visual/interactive. Run dev server, navigate to /dashboard/kpi, click CIL bar and Carteira card.</automated>
  </verify>

  <done>CIL chart/modal e Carteira modal operacionais com todos os estados</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Fetch requests com parâmetros de período |
| Client → File System | CSV export via Blob/URL (nenhum dado sai do cliente) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | I | CilModal — Estado filter | mitigate | Filter value mapped to API query param directly (no transformation needed — estado is a valid column value) |
| T-03-05 | T | CarteiraModal — numeric input | mitigate | Input type="number" com min=0 e parseInt no submit garante integer positivo |
| T-03-06 | S | CSV Export | accept | Dados já estão no cliente (não há exfiltração); é funcionalidade deliberada do usuário |
</threat_model>

<verification>
1. Dev server: `npm run dev` sem erros
2. Navegar para /dashboard/kpi — página carrega sem crash
3. Verificar filtro de período: dropdown de mês reflete dados
4. Verificar cards: 5 cards com valores formatados
5. Verificar gráficos: 4 gráficos renderizam
6. Verificar CIL modal: clicar barra → modal abre → filtrar por estado → exportar CSV
7. Verificar carteira modal: clicar no card → editar → salvar
8. Upload: fazer upload de CSV → toast com "Ver Dashboard"
</verification>

<success_criteria>
1. Página /dashboard/kpi funcional com todos os componentes
2. Dados carregados via fetch das APIs do Plano 01
3. Período filtrável: mês e date range sincronizados
4. 5 KPI cards com valores reais formatados em pt-PT
5. 4 gráficos Recharts com dados dinâmicos
6. CIL modal com tabela, filtro e export CSV
7. Carteira editável com persistência em SQLite
8. Upload com navegação rápida para o dashboard
</success_criteria>

<output>
After completion, create `.planning/phases/03-kpi-dashboard/03-02-SUMMARY.md`
</output>
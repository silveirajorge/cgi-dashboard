---
phase: 03-kpi-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/navigation/sidebar/sidebar-items.ts
  - src/lib/db.ts
  - src/app/api/data/route.ts
  - src/app/api/cil/route.ts
  - src/app/api/cil/[cil]/route.ts
  - src/app/api/carteira/route.ts
autonomous: true
requirements: [KPI-01, KPI-02, KPI-03]

must_haves:
  truths:
    - "Sidebar exibe grupo 'Pedidos e Tendências' com itens Dashboard e Upload CSV"
    - "Item 'Upload CSV' não aparece mais no grupo Dashboards"
    - "GET /api/data aceita ?from=&to= e retorna total, canais, daily, meses_disponiveis, crescimento, workdays, avg_daily, max_day, tipologias_por_canal"
    - "GET /api/cil?from=&to= retorna top 20 CILs agregados"
    - "GET /api/cil/[cil]?from=&to=&estado= retorna registros do CIL no período"
    - "GET /api/carteira?mes=YYYY-MM retorna total_clientes ou null"
    - "POST /api/carteira salva/atualiza carteira_clientes com UPSERT"
    - "Tabela carteira_clientes existe no SQLite com colunas id, mes_competencia, total_clientes"
    - "Índice em pedidos(cil) para performance de queries CIL"
  artifacts:
    - path: "src/navigation/sidebar/sidebar-items.ts"
      provides: "Sidebar navigation structure with new group"
      contains: "Pedidos e Tendências"
    - path: "src/lib/db.ts"
      provides: "carteira_clientes table migration + CIL index"
      contains: "carteira_clientes"
    - path: "src/app/api/data/route.ts"
      provides: "Enhanced GET /api/data with period filtering"
      exports: ["GET"]
    - path: "src/app/api/cil/route.ts"
      provides: "GET /api/cil with period filtering"
      exports: ["GET"]
    - path: "src/app/api/cil/[cil]/route.ts"
      provides: "GET /api/cil/[cil] with period and estado filtering"
      exports: ["GET"]
    - path: "src/app/api/carteira/route.ts"
      provides: "GET + POST /api/carteira"
      exports: ["GET", "POST"]
  key_links:
    - from: "GET /api/data"
      to: "pedidos table"
      via: "SQL WHERE with date range"
      pattern: "dt_registo >= ? AND dt_registo <= ?"
    - from: "GET /api/cil"
      to: "pedidos table"
      via: "SQL GROUP BY cil"
      pattern: "GROUP BY cil"
    - from: "POST /api/carteira"
      to: "carteira_clientes table"
      via: "INSERT OR REPLACE"
      pattern: "INSERT OR REPLACE"
---

<objective>
Infraestrutura de backend para o dashboard de KPIs: reestruturação da sidebar, migração do banco SQLite (tabela carteira_clientes + índice CIL), e criação/melhoria das APIs de dados com filtro por período.

Purpose: Fornecer todos os endpoints que o frontend do dashboard consumirá, com dados agregados via SQL para performance com 90k+ registros.
Output: Sidebar reestruturada, banco migrado, 5 endpoints de API operacionais.
</objective>

<execution_context>
@/home/jsilveira/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/jsilveira/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-kpi-dashboard/03-CONTEXT.md
@.planning/phases/03-kpi-dashboard/03-RESEARCH.md

</context>

<tasks>

<task type="auto">
  <name>Task 1: Reestruturar sidebar + migrar banco SQLite</name>

  <read_first>
  - .planning/phases/03-kpi-dashboard/03-CONTEXT.md (D-01, D-02, D-03, D-28)
  - src/navigation/sidebar/sidebar-items.ts (estrutura atual dos grupos)
  - src/lib/db.ts (migrations existentes, padrão getDb)
  </read_first>

  <files>
  src/navigation/sidebar/sidebar-items.ts,
  src/lib/db.ts
  </files>

  <action>
  1.1 — src/navigation/sidebar/sidebar-items.ts — Reestruturar sidebar (per D-01, D-02, D-03):
    a. NOVO GRUPO "Pedidos e Tendências" com id entre 1 (Dashboards) e 2 (Pages):
       - id: 2 (re-number ids: Dashboards=1, Pedidos e Tendências=2, Pages=3, Legacy=4, Misc=5)
       - label: "Pedidos e Tendências"
       - items: [
           { title: "Dashboard", url: "/dashboard/kpi", icon: BarChart3 },
           { title: "Upload CSV", url: "/dashboard/upload", icon: Upload }
         ]
    b. REMOVER o item "Upload CSV" do array items do grupo Dashboards (id=1) — manter apenas os itens originais (Default, CRM, Finance, Analytics, Productivity, E-commerce, Academy, Logistics)
    c. Adicionar import de BarChart3 de "lucide-react" (ainda não está importado)
    d. Renumerar TODOS os ids sequencialmente: Dashboards=1, Pedidos e Tendências=2, Pages=3, Legacy=4, Misc=5

  1.2 — src/lib/db.ts — Migração do banco (per D-28):
    a. Em runMigrations(), ADICIONAR após a criação de upload_history:
       ```typescript
       database.exec(`
         CREATE TABLE IF NOT EXISTS carteira_clientes (
           id INTEGER PRIMARY KEY AUTOINCREMENT,
           mes_competencia TEXT NOT NULL UNIQUE,
           total_clientes INTEGER NOT NULL,
           created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
           updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
         )
       `);

       // Índice em CIL para performance de queries CIL
       database.exec(`
         CREATE INDEX IF NOT EXISTS idx_pedidos_cil ON pedidos(cil)
       `);
       ```
    b. NOTA: CREATE INDEX usa IF NOT EXISTS — seguro executar múltiplas vezes (migrations idempotentes)
  </action>

  <acceptance_criteria>
  1. Sidebar exibe grupo "Pedidos e Tendências" com ícone BarChart3 e Upload
  2. "Upload CSV" não aparece mais no grupo "Dashboards"
  3. Índices dos grupos são sequenciais: 1, 2, 3, 4, 5
  4. Banco .data/cgi.db tem tabela carteira_clientes com schema correto
  5. Índice idx_pedidos_cil existe na tabela pedidos
  </acceptance_criteria>

  <verify>
    <automated>grep -c "Pedidos e Tendências" src/navigation/sidebar/sidebar-items.ts && grep -c "BarChart3" src/navigation/sidebar/sidebar-items.ts && grep -c "carteira_clientes" src/lib/db.ts</automated>
  </verify>

  <done>Sidebar reestruturada, migrations executadas, banco com carteira_clientes + índice CIL</done>
</task>

<task type="auto">
  <name>Task 2: Aprimorar GET /api/data com filtro de período + métricas agregadas</name>

  <read_first>
  - .planning/phases/03-kpi-dashboard/03-CONTEXT.md (D-20, D-21, D-22, D-33, D-42 a D-46)
  - .planning/phases/03-kpi-dashboard/03-RESEARCH.md (Pattern 1, Column Analysis, Pitfalls 1, 2, 5)
  - src/app/api/data/route.ts (implementação atual)
  - src/lib/services/pedidos.ts (funções existentes)
  </read_first>

  <files>
  src/app/api/data/route.ts
  </files>

  <action>
  Reescrever GET /api/data para aceitar query params ?from=YYYY-MM-DD&to=YYYY-MM-DD e retornar métricas agregadas.

  2.1 — Importar { NextRequest } de "next/server":
    ```typescript
    import { type NextRequest, NextResponse } from "next/server";
    ```

  2.2 — GET handler modificar para:
    a. Extrair searchParams:
       ```typescript
       export async function GET(request: NextRequest) {
         const { searchParams } = new URL(request.url);
         const from = searchParams.get("from");
         const to = searchParams.get("to");
       ```
    b. Construir WHERE dinâmico:
       - Se from e to presentes: `WHERE dt_registo >= ? AND dt_registo <= ?` com params [from, to + " 23:59:59"]
       - Se não: sem WHERE (todos os dados)
    c. Se tabela pedidos não existir, retornar objeto vazio (total:0, canais:[], etc.)

    d. Queries SQL (todas no mesmo db, reutilizar where + params):
       - **total**: `SELECT COUNT(*) as total FROM pedidos ${where}`
       - **canais** (CASE WHEN normalization — per D-21, RESEARCH.md channel mapping):
         ```sql
         SELECT
           CASE
             WHEN "receção" IN ('Portal', 'Portal da ERSE', 'Portal Queixa', 'Formulário Site Público') THEN 'Portal'
             WHEN "receção" IN ('Back-Office', 'Telefone Back-Office') THEN 'Back-Office'
             WHEN "receção" = 'Email' THEN 'Email'
             WHEN "receção" = 'Telefone Front' THEN 'Telefone Front'
             ELSE 'Outros'
           END as canal,
           COUNT(*) as count
         FROM pedidos ${where}
         GROUP BY canal
         ORDER BY count DESC
         ```
       - **daily** (SUBSTR dt_contato — per RESEARCH.md Pitfall 5):
         ```sql
         SELECT SUBSTR(dt_contato, 1, 10) as date, COUNT(*) as count
         FROM pedidos ${where}
         GROUP BY date ORDER BY date
         ```
       - **meses_disponiveis**: `SELECT DISTINCT mes_competencia FROM pedidos ORDER BY mes_competencia`
       - **crescimento** (per D-42 a D-46): Calcular período anterior de mesma duração:
         ```typescript
         function getPreviousPeriod(from: string, to: string) {
           const duration = new Date(to).getTime() - new Date(from).getTime();
           const prevTo = new Date(new Date(from).getTime() - 86400000).toISOString().split("T")[0];
           const prevFrom = new Date(new Date(prevTo).getTime() - duration).toISOString().split("T")[0];
           return { prevFrom, prevTo };
         }
         ```
         Query período anterior: mesma query de total com where nos parâmetros anteriores.
         Fórmula: `growth = prev_total > 0 ? (current - prev) / prev * 100 : null`
         `prev_label`: "vs. Mês Anterior" se período = 1 mês, "vs. Período Anterior" caso contrário
       - **workdays**: função countWorkdays(from, to) — iterar datas excluindo sábado(6) e domingo(0)
       - **avg_daily**: total / workdays (se workdays > 0, senão 0)
       - **max_day**: `SELECT SUBSTR(dt_contato, 1, 10) as date, COUNT(*) as count FROM pedidos ${where} GROUP BY date ORDER BY count DESC LIMIT 1`
       - **tipologias_por_canal** (per D-22): Para cada canal normalizado, top 5 justificação:
         ```sql
         SELECT
           CASE WHEN "receção" IN ('Portal','Portal da ERSE','Portal Queixa','Formulário Site Público') THEN 'Portal'
                WHEN "receção" IN ('Back-Office','Telefone Back-Office') THEN 'Back-Office'
                WHEN "receção" = 'Email' THEN 'Email'
                WHEN "receção" = 'Telefone Front' THEN 'Telefone Front'
                ELSE 'Outros'
           END as canal,
           justificação,
           COUNT(*) as count
         FROM pedidos ${where}
         GROUP BY canal, justificação
         ORDER BY canal, count DESC
         ```
         No servidor, agrupar por canal e pegar top 5 de cada.

    e. CRÍTICO — Usar aspas duplas `"receção"` na SQL (coluna tem cedilha — RESEARCH.md Pitfall 1). Usar `justificação` (não `tipo_justificação` — RESEARCH.md Pitfall 2).

    f. Response JSON shape:
    ```typescript
    return NextResponse.json({
      total,
      canais: [{ canal: string, count: number }],
      daily: [{ date: string, count: number }],
      meses_disponiveis: [{ mes_competencia: string }],
      crescimento: { value: number | null, label: string | null },
      workdays: number,
      avg_daily: number,
      max_day: { date: string, count: number } | null,
      tipologias_por_canal: Record<string, Array<{ justificacao: string, count: number }>>,
    });
    ```

  g. Tratamento de erro: try/catch retornando { error } com status 500.
  </action>

  <acceptance_criteria>
  1. GET /api/data sem params retorna todos os dados com canais, daily, meses_disponiveis
  2. GET /api/data?from=2026-01-01&to=2026-01-31 retorna apenas dados de Janeiro
  3. Canais normalizados: Back-Office, Email, Portal, Telefone Front, Outros
  4. daily usa SUBSTR(dt_contato,1,10) — datas sem timestamp
  5. crescimento calcula percentual vs período anterior
  6. workdays exclui sábado e domingo
  7. tipologias_por_canal contém top 5 justificação por canal
  </acceptance_criteria>

  <verify>
    <automated>curl -s "http://localhost:3000/api/data?from=2026-01-01&to=2026-01-31" | head -c 200</automated>
  </verify>

  <done>GET /api/data aprimorado com todas as métricas agregadas e filtro de período</done>
</task>

<task type="auto">
  <name>Task 3: Criar APIs de CIL e Carteira de Clientes</name>

  <read_first>
  - .planning/phases/03-kpi-dashboard/03-CONTEXT.md (D-34, D-35, D-36, D-37)
  - .planning/phases/03-kpi-dashboard/03-RESEARCH.md (Pattern 1, Technical Approach)
  - src/app/api/data/route.ts (padrão de NextRequest + NextResponse já usado na Task 2)
  </read_first>

  <files>
  src/app/api/cil/route.ts,
  src/app/api/cil/[cil]/route.ts,
  src/app/api/carteira/route.ts
  </files>

  <action>
  Criar três novos endpoints de API.

  3.1 — src/app/api/cil/route.ts — GET /api/cil (per D-34):
    ```typescript
    import { type NextRequest, NextResponse } from "next/server";
    import { getDb } from "@/lib/db";

    export async function GET(request: NextRequest) {
      try {
        const { searchParams } = new URL(request.url);
        const from = searchParams.get("from");
        const to = searchParams.get("to");

        const db = getDb();
        const tableExists = db.prepare("SELECT name FROM sqlite_master WHERE type='table' AND name='pedidos'").get();
        if (!tableExists) {
          return NextResponse.json({ cil_aggregates: [] });
        }

        const conditions: string[] = [];
        const params: string[] = [];
        if (from && to) {
          conditions.push("dt_registo >= ? AND dt_registo <= ?");
          params.push(from, to + " 23:59:59");
        }
        const where = conditions.length ? `WHERE ${conditions.join(" AND ")}` : "";

        const cilAggregates = db.prepare(`
          SELECT cil, COUNT(*) as count
          FROM pedidos
          ${where}
          GROUP BY cil
          ORDER BY count DESC
          LIMIT 20
        `).all(...params);

        return NextResponse.json({ cil_aggregates: cilAggregates });
      } catch (error) {
        const message = error instanceof Error ? error.message : "Erro interno";
        return NextResponse.json({ error: message }, { status: 500 });
      }
    }
    ```

  3.2 — src/app/api/cil/[cil]/route.ts — GET /api/cil/[cil] (per D-35):
    ```typescript
    import { type NextRequest, NextResponse } from "next/server";
    import { getDb } from "@/lib/db";

    export async function GET(
      request: NextRequest,
      { params }: { params: Promise<{ cil: string }> }
    ) {
      try {
        const { cil } = await params;
        const { searchParams } = new URL(request.url);
        const from = searchParams.get("from");
        const to = searchParams.get("to");
        const estado = searchParams.get("estado");

        const db = getDb();

        const conditions: string[] = ['cil = ?'];
        const queryParams: string[] = [cil];

        if (from && to) {
          conditions.push("dt_registo >= ? AND dt_registo <= ?");
          queryParams.push(from, to + " 23:59:59");
        }
        if (estado) {
          conditions.push("estado = ?");
          queryParams.push(estado);
        }

        const where = `WHERE ${conditions.join(" AND ")}`;

        const total = db.prepare(`SELECT COUNT(*) as total FROM pedidos ${where}`).get(...queryParams) as { total: number };

        const records = db.prepare(`
          SELECT * FROM pedidos ${where}
          ORDER BY dt_registo DESC
          LIMIT 1000
        `).all(...queryParams);

        return NextResponse.json({ total: total.total, records });
      } catch (error) {
        const message = error instanceof Error ? error.message : "Erro interno";
        return NextResponse.json({ error: message }, { status: 500 });
      }
    }
    ```
    NOTA: Usar `params: Promise<{ cil: string }>` — Next.js 16 usa params assíncrono.

  3.3 — src/app/api/carteira/route.ts — GET + POST /api/carteira (per D-32, D-36, D-37):
    ```typescript
    import { type NextRequest, NextResponse } from "next/server";
    import { getDb } from "@/lib/db";

    export async function GET(request: NextRequest) {
      try {
        const { searchParams } = new URL(request.url);
        const mes = searchParams.get("mes");

        if (!mes) {
          return NextResponse.json({ error: "Parâmetro 'mes' é obrigatório (YYYY-MM)" }, { status: 400 });
        }

        const db = getDb();
        const row = db.prepare(
          "SELECT total_clientes FROM carteira_clientes WHERE mes_competencia = ?"
        ).get(mes) as { total_clientes: number } | undefined;

        return NextResponse.json({ total_clientes: row?.total_clientes ?? null });
      } catch (error) {
        const message = error instanceof Error ? error.message : "Erro interno";
        return NextResponse.json({ error: message }, { status: 500 });
      }
    }

    export async function POST(request: NextRequest) {
      try {
        const body = await request.json();
        const { mes_competencia, total_clientes } = body;

        if (!mes_competencia || total_clientes === undefined) {
          return NextResponse.json(
            { error: "Campos obrigatórios: mes_competencia (YYYY-MM), total_clientes (number)" },
            { status: 400 }
          );
        }

        if (typeof total_clientes !== "number" || !Number.isInteger(total_clientes) || total_clientes < 0) {
          return NextResponse.json(
            { error: "total_clientes deve ser um número inteiro positivo" },
            { status: 400 }
          );
        }

        const db = getDb();
        db.prepare(`
          INSERT INTO carteira_clientes (mes_competencia, total_clientes)
          VALUES (?, ?)
          ON CONFLICT(mes_competencia) DO UPDATE SET
            total_clientes = excluded.total_clientes,
            updated_at = CURRENT_TIMESTAMP
        `).run(mes_competencia, total_clientes);

        return NextResponse.json({ success: true, mes_competencia, total_clientes });
      } catch (error) {
        const message = error instanceof Error ? error.message : "Erro interno";
        return NextResponse.json({ error: message }, { status: 500 });
      }
    }
    ```

    NOTA: Usar `ON CONFLICT...DO UPDATE` (SQLite 3.24+) em vez de INSERT OR REPLACE — preserva created_at na primeira inserção.
  </action>

  <acceptance_criteria>
  1. GET /api/cil?from=2026-01-01&to=2026-01-31 retorna top 20 CILs com count
  2. GET /api/cil/91619?from=2026-01-01&to=2026-01-31 retorna registros + total
  3. GET /api/cil/91619?from=2026-01-01&to=2026-01-31&estado=Fechado filtra por estado
  4. GET /api/carteira?mes=2026-01 retorna total_clientes ou null
  5. POST /api/carteira com { mes_competencia: "2026-01", total_clientes: 500 } salva e retorna success
  6. POST duplicado para mesmo mês atualiza em vez de criar duplicata
  7. Validação: total_clientes deve ser inteiro positivo
  </acceptance_criteria>

  <verify>
    <automated>curl -s "http://localhost:3000/api/cil?from=2026-01-01&to=2026-01-31" | head -c 200 && curl -s "http://localhost:3000/api/carteira?mes=2026-01" | head -c 100</automated>
  </verify>

  <done>APIs de CIL (agregação + detalhes) e Carteira (GET + POST) operacionais</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Dados de KPI via HTTP GET |
| API → SQLite | Queries SQL parametrizadas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I | GET /api/data | mitigate | Validar formato dos parâmetros from/to (YYYY-MM-DD regex) — implementado via SQLite type coercion segura com parâmetros posicionais |
| T-03-02 | I | POST /api/carteira | mitigate | Validar body JSON com type check (total_clientes deve ser number inteiro) antes de inserir |
| T-03-03 | I | GET /api/cil/[cil] | mitigate | CIL é string, usado como parâmetro posicional (não concatenado na SQL) — sem risco de SQL injection |
</threat_model>

<verification>
1. Build: `npx next build` sem erros (fazer build se dev server estiver parado)
2. Lint: `npx biome check src/navigation/sidebar/sidebar-items.ts src/app/api/` sem erros
3. API: Testar cada endpoint com curl
4. Banco: Verificar tabela carteira_clientes com sqlite3 .data/cgi.db ".tables"
</verification>

<success_criteria>
1. Sidebar reestruturada com grupo "Pedidos e Tendências"
2. Banco migrado com carteira_clientes + índice CIL
3. GET /api/data responde com todos os campos (total, canais, daily, meses, crescimento, workdays, avg_daily, max_day, tipologias)
4. GET /api/cil responde com top 20 CILs
5. GET /api/cil/[cil] responde com registros filtrados
6. GET/POST /api/carteira funcionam com UPSERT
</success_criteria>

<output>
After completion, create `.planning/phases/03-kpi-dashboard/03-01-SUMMARY.md`
</output>
---
phase: 04.2-formulario-de-avaliacao
plan: 01
subsystem: api, ui
tags: sqlite, nextjs, shadcn, combobox, better-sqlite3

requires:
  - phase: 04.1-cadastro-de-funcionarios
    provides: funcionarios table, sidebar item "Avaliação"
provides:
  - avaliacoes SQLite table with GENERATED column for automatic average
  - API routes: GET /api/avaliacoes (list with filters), POST /api/avaliacoes (create), GET /api/avaliacoes/[id] (detail)
  - Page /dashboard/avaliacao with list/form mode state
  - Inline form with funcionário combobox, 6 numeric inputs (1-10), date, comment, live average preview
  - Readonly detail modal with all category grades and average
  - Table with filters (funcionário dropdown + date range)
affects:
  - Fase 5: Dashboard do Grupo (uses avaliacoes data)
  - Fase 6: Histórico por Funcionário (uses avaliacoes data)

tech-stack:
  added: []
  patterns:
    - SQLite GENERATED ALWAYS AS ... STORED column for computed averages
    - shadcn Combobox (@base-ui/react) for searchable dropdowns
    - Inline form mode state pattern (list/form) replacing list with form

key-files:
  created:
    - src/app/api/avaliacoes/route.ts
    - src/app/api/avaliacoes/[id]/route.ts
    - src/app/(main)/dashboard/avaliacao/page.tsx
    - src/app/(main)/dashboard/avaliacao/_components/avaliacoes-page-client.tsx
    - src/app/(main)/dashboard/avaliacao/_components/avaliacoes-table.tsx
    - src/app/(main)/dashboard/avaliacao/_components/avaliacao-form.tsx
    - src/app/(main)/dashboard/avaliacao/_components/avaliacao-detail-modal.tsx
  modified:
    - src/lib/db.ts

key-decisions:
  - "SQLite GENERATED column (STORED) for average — single source of truth, auto-calculated on INSERT"
  - "Inline form replaces list via mode state (not route or modal) per D4.2-07"
  - "Combobox uses @base-ui/react via shadcn add — searchable dropdown without extra npm installs"
  - "Native <select> for filter dropdown (simple) vs Combobox for form (needs search)"

patterns-established:
  - "Mode state pattern: list/form toggling in a single page client component"
  - "SQLite GENERATED ALWAYS AS STORED for computed columns that must be consistent"

requirements-completed: [SC-01, SC-02, SC-03, SC-04, SC-05, SC-06, SC-07, SC-08, SC-09]

duration: 4min
completed: 2026-06-07
---

# Phase 04.2 Plan 01: Formulário de Avaliação — Summary

**SQLite migration with GENERATED column for average, 3 API endpoints, inline form with combobox + 6 numeric inputs, list/table with filters, and readonly detail modal**

## Performance

- **Duration:** 4 min
- **Started:** 2026-06-07T08:49:22Z
- **Completed:** 2026-06-07T08:53:XXZ
- **Tasks:** 3
- **Files modified:** 8

## Accomplishments

- Migração SQLite da tabela `avaliacoes` com 6 categorias (1-10), GENERATED column `media`, CHECK constraints e FOREIGN KEY
- API endpoints com prepared statements, validação backend, JOIN com funcionários e filtros dinâmicos
- Página `/dashboard/avaliacao` com server component, client component com mode state (list/form)
- Tabela de avaliações com filtros (dropdown funcionário + date range), loading/error/empty states
- Formulário inline com Combobox searchável, 6 inputs numéricos (grid 2-col), date picker, média preview ao vivo, comentário opcional
- Modal de detalhes apenas-leitura com notas por categoria, média e comentário
- Validação tripla: HTML (min/max/step), client-side (antes do POST), backend (antes do INSERT)

## Task Commits

Each task was committed atomically:

1. **Task 1: Migração SQLite + API routes** - `88c5b4a` (feat)
2. **Task 2: Combobox + page + page-client + table** - `73c264e` (feat)
3. **Task 3: Form + detail modal** - `813bd36` (feat)

**Plan metadata:** (pending)

## Files Created/Modified

- `src/lib/db.ts` - Added `CREATE TABLE IF NOT EXISTS avaliacoes` with GENERATED column
- `src/app/api/avaliacoes/route.ts` - GET (list with filters) + POST (create, no media in INSERT)
- `src/app/api/avaliacoes/[id]/route.ts` - GET (detail with JOIN)
- `src/app/(main)/dashboard/avaliacao/page.tsx` - Server component
- `src/app/(main)/dashboard/avaliacao/_components/avaliacoes-page-client.tsx` - Mode state (list/form)
- `src/app/(main)/dashboard/avaliacao/_components/avaliacoes-table.tsx` - Table with filters
- `src/app/(main)/dashboard/avaliacao/_components/avaliacao-form.tsx` - Inline form with combobox
- `src/app/(main)/dashboard/avaliacao/_components/avaliacao-detail-modal.tsx` - Readonly detail modal

## Decisions Made

- All decisions followed the plan exactly — no architectural changes needed
- Combobox API from @base-ui/react uses slightly different types than documented (onValueChange accepts null)
- The sidebar item "Avaliação" already existed without `comingSoon` — no sidebar changes needed

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- Biome lint `useExhaustiveDependencies` flagged `refreshKey` as unnecessary dependency — suppressed with biome-ignore comment since it's intentionally used to trigger refetch
- Biome `noLabelWithoutControl` a11y rule required adding `htmlFor`/`id` attributes to filter labels — fixed inline
- Combobox `onValueChange` type expects `FuncionarioOption | null` not just `FuncionarioOption` — fixed type signature

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 4.2 complete and ready for Fase 5 (Dashboard do Grupo) and Fase 6 (Histórico por Funcionário)
- Data model `avaliacoes` with funcionario_id foreign key enables future GROUP BY and per-employee queries

## Self-Check: PASSED

All 8 files verified on disk. All 3 commits verified in git log. SQLite migration with GENERATED column confirmed.

---

*Phase: 04.2-formulario-de-avaliacao*
*Completed: 2026-06-07*
